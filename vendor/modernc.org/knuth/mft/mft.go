// Code generated by '[/tmp/go-build4045508623/b001/exe/generate]', DO NOT EDIT.

// % This program by D. E. Knuth is not copyrighted and can be used freely.
// % Version 0.0 was more-or-less debugged on June 4, 1985.
// % Version 0.1 improved formatting of : and added \\ (June 15, 1985).
// % Version 0.2 improved formatting of good, fixed @ bug (August 4, 1985).
// % Version 0.3 fixed minor bug in change_file move (August 30, 1985).
// % Version 0.4 fixed minor bug regarding empty comments (April 8, 1989).
// % Version 1.0 was tuned up for the METAFONTware report (April 16, 1989).
// % Version 1.1 ditto, with input handled by Hosek's idea (April 27, 1989).
// % Version 2 has the new primitives of METAFONT 2.0 (October 16, 1989).
// % Version 2.1 corrects two of those primitives (January 20, 2021).
//
// % Here is TeX material that gets inserted after \input webmac
// \def\hang[\hangindent 3em\indent\ignorespaces]
// \font\ninerm=cmr9
// \let\mc=\ninerm % medium caps for names like SAIL
// \def\PASCAL[Pascal]
// \font\logo=manfnt % font used for the METAFONT logo
// \def\MF[[\logo META]\-[\logo FONT]]
// \def\pb[$\.|\ldots\.|$] % MF brackets (|...|)
// \def\v[\.[\char'174]] % vertical (|) in typewriter font
// \def\dleft[[\![] \def\dright[]\!]] % double brackets
// \mathchardef\RA="3221 % right arrow
// \mathchardef\BA="3224 % double arrow
// \def\([] % kludge for alphabetizing certain module names
//
// \def\title[MFT]
// \def\contentspagenumber[401]
// \def\topofcontents[\null
//   \titlefalse % include headline on the contents page
//   \def\rheader[\mainfont\hfil \contentspagenumber]
//   \vfill
//   \centerline[\titlefont The [\ttitlefont MFT] processor]
//   \vskip 15pt
//   \centerline[(Version 2.1, January 2021)]
//   \vfill]
// \def\botofcontents[\vfill
//   \centerline[\hsize 5in\baselineskip9pt
//     \vbox[\ninerm\noindent
//     The preparation of this report
//     was supported in part by the National Science
//     Foundation under grants IST-8201926, MCS-8300984, and
//     CCR-8610181,
//     and by the System Development Foundation. `\TeX' is a
//     trademark of the American Mathematical Society.
//     `[\logo hijklmnj]\kern1pt' is a trademark of Addison-Wesley
//     Publishing Company.]]]
// \pageno=\contentspagenumber \advance\pageno by 1
//
//

// 1. Introduction

// tangle:pos mft.web:49:17:

// This program converts a \MF\ source file to a \TeX\ file. It was written
// by D.~E. Knuth in June, 1985; a somewhat similar [\mc SAIL] program had
// \xref[Knuth, Donald Ervin]
// been developed in January, 1980.
//
// The general idea is to input a file called, say, \.[foo.mf] and to produce an
// output file called, say, \.[foo.tex]. The latter file, when processed by \TeX,
// will yield a ``prettyprinted'' representation of the input file.
// \xref[user manual]
//
// Line breaks in the input are carried over into the output; moreover,
// blank spaces at the beginning of a line are converted to quads of indentation
// in the output. Thus, the user has full control over the indentation and line
// breaks. Each line of input is translated independently of the others.
//
// A slight change to \MF's comment convention allows further control.
// Namely, `\.[\%\%]' indicates that the remainder of an input line should be
// copied verbatim to the output; this interrupts the translation and forces
// \.[MFT] to produce a certain result.
//
// Furthermore, `\.[\%\%\%] $\langle\,$token$_1\,\rangle\ldots
// \langle\,$token$_n\,\rangle$'
// introduces a change in \.[MFT]'s formatting rules; all tokens after the first
// will henceforth be translated according to the current conventions for
// $\langle\,$token$_1\,\rangle$. The tokens must be symbolic (i.e., not
// numeric or string tokens). For example, the input line
// $$\.[\%\%\% addto fill draw filldraw]$$
// says that the `\.[fill]', `\.[draw]', and `\.[filldraw]' operations of
// plain \MF\ should be formatted as the primitive token `\.[addto]', i.e.,
// in boldface type. (Without such reformatting commands, \.[MFT] would treat
// `\.[fill]' like an ordinary tag or variable name. In fact, you need
// a reformatting command even to get parentheses to act like delimiters!)
//
// \MF\ comments, which follow a single \.\% sign, should be valid \TeX\
// input.  But \MF\ material can be included in \pb\ within a comment; this
// will be translated by \.[MFT] as if it were not in a comment. For example,
// a phrase like `\.[make] \.[\v x2r\v] \.[zero]' will be translated into
// `\.[make \$x\_\[2r\]\$ zero]'.
//
// The rules just stated apply to lines that contain one, two, or three \.\% signs
// in a row. Comments to \.[MFT] can follow `\.[\%\%\%\%]'.
// Five or more \.\% signs should not be used.
//
// Beside the normal input file, \.[MFT] also looks for a change file
// (e.g., `\.[foo.ch]'), which allows substitutions to be made in the
// translation. The change file follows the conventions of \.[WEB], and
// it should be null if there are no changes. (Changes usually contain
// verbatim instructions to compensate for the fact that \.[MFT] cannot
// format everything in an optimum way.)
//
// There's also a third input file (e.g., `\.[plain.mft]'), which is
// input before the other two. This file normally contains the `\.[\%\%\%]'
// formatting commands that are necessary to tune \.[MFT] to a particular
// style of \MF\ code, so it is called the style file.
//
// The output of \.[MFT] should be accompanied by the macros in a small
// package called \.[mftmac.tex].
// \xref[mftmac]
//
// Caveat: This program is not as ``bulletproof'' as the other routines
// produced by Stanford's \TeX\ project. It takes care of a great deal of
// tedious formatting, but it can produce strange output, because \MF\ is
// an extremely general language. Users should proofread their output carefully.

// 2.

// tangle:pos mft.web:114:3:

// \.[MFT] uses a few features of the local \PASCAL\ compiler that may
// need to be changed in other installations:
//
// \yskip\item[1)] Case statements have a default.
// \item[2)] Input-output routines may need to be adapted for use with a particular
// character set and/or for printing messages on the user's terminal.
//
// \yskip\noindent
// These features are also present in the \PASCAL\ version of \TeX, where they
// are used in a similar (but more complex) way. System-dependent portions
// of \.[MFT] can be identified by looking at the entries for `system
// dependencies' in the index below.
//  \xref[system dependencies]
//
// The ``banner line'' defined here should be changed whenever \.[MFT]
// is modified.

// 3.

// tangle:pos mft.web:133:3:

// The program begins with a fairly normal header, made up of pieces that
// \xref[system dependencies]
// will mostly be filled in later. The \.[MF] input comes from files |mf_file|,
// |change_file|, and |style_file|; the \TeX\ output goes to file |tex_file|.
//
// If it is necessary to abort the job because of a fatal error, the program
// calls the `|jump_out|' procedure, which goes to the label |end_of_MFT|.
// \4
// Compiler directives
// $C+,A+,D-
// range check, catch arithmetic overflow, no debug overhead

package mft

import (
	"math"
	"unsafe"

	"modernc.org/knuth"
)

var (
	_ = math.MaxInt32
	_ unsafe.Pointer
)

type (
	char   = byte
	signal int
)

func strcopy(dst []char, src string) {
	for i := 0; i < len(dst) && i < len(src); i++ {
		dst[i] = src[i]
	}
}

func arraystr(a []char) string {
	b := make([]byte, len(a))
	for i, c := range a {
		b[i] = c
	}
	return string(b)
}

func abs(n int32) int32 {
	if n >= 0 {
		return n
	}

	return -n
}

func fabs(f float64) float64 {
	if f >= 0 {
		return f
	}

	return -f
}

func round(f float64) int32 {
	if f >= 0 {
		return int32(f + 0.5)
	}

	return int32(f - 0.5)
}

const (
	endOfMft          = 9999 /* go here to wrap it up */
	exit              = 10   /* go here to leave a procedure */
	restart           = 20   /* go here to start a procedure again */
	reswitch          = 21   /* go here to start a case statement again */
	continue1         = 22   /* go here to resume a loop */
	done              = 30   /* go here to exit a loop */
	found             = 31   /* go here when you've found it */
	notFound          = 32   /* go here when you've found something else */
	spotless          = 0    /* |history| value for normal jobs */
	harmlessMessage   = 1    /* |history| value when non-serious info was printed */
	errorMessage      = 2    /* |history| value when an error was noted */
	fatalMessage      = 3    /* |history| value when we had to stop prematurely */
	firstTextChar     = 0    /* ordinal number of the smallest element of |text_char| */
	lastTextChar      = 255  /* ordinal number of the largest element of |text_char| */
	indentation       = 0    /* internal code for space at beginning of a line */
	endOfLine         = 1    /* internal code for hypothetical token at end of a line */
	endOfFile         = 2    /* internal code for hypothetical token at end of the input */
	verbatim          = 3    /* internal code for the token `\.[\%\%]' */
	setFormat         = 4    /* internal code for the token `\.[\%\%\%]' */
	mftComment        = 5    /* internal code for the token `\.[\%\%\%\%]' */
	minActionType     = 6    /* smallest code for tokens that produce ``real'' output */
	numericToken      = 6    /* internal code for tokens like `\.[3.14159]' */
	stringToken       = 7    /* internal code for tokens like `|"pie"|' */
	minSymbolicToken  = 8    /* smallest internal code for a symbolic token */
	op                = 8    /* internal code for tokens like `\.[sqrt]' */
	command           = 9    /* internal code for tokens like `\.[addto]' */
	endit             = 10   /* internal code for tokens like `\.[fi]' */
	binary            = 11   /* internal code for tokens like `\.[and]' */
	abinary           = 12   /* internal code for tokens like `\.[+]' */
	bbinary           = 13   /* internal code for tokens like `\.[step]' */
	ampersand         = 14   /* internal code for the token `\.[\char`\&]' */
	pythSub           = 15   /* internal code for the token `\.[+-+]' */
	asIs              = 16   /* internal code for tokens like `\.[]]' */
	bold              = 17   /* internal code for tokens like `\.[nullpen]' */
	typeName          = 18   /* internal code for tokens like `\.[numeric]' */
	pathJoin          = 19   /* internal code for the token `\.[..]' */
	colon             = 20   /* internal code for the token `\.:' */
	semicolon         = 21   /* internal code for the token `\.;' */
	backslash         = 22   /* internal code for the token `\.[\\]' */
	doubleBack        = 23   /* internal code for the token `\.[\\\\]' */
	lessOrEqual       = 24   /* internal code for the token `\.[<=]' */
	greaterOrEqual    = 25   /* internal code for the token `\.[>=]' */
	notEqual          = 26   /* internal code for the token `\.[<>]' */
	sharp             = 27   /* internal code for the token `\.[\char`\#]' */
	comment           = 28   /* internal code for the token `\.[\char`\%]' */
	recomment         = 29   /* internal code used to resume a comment after `\pb' */
	minSuffix         = 30   /* smallest code for symbolic tokens in suffixes */
	internal          = 30   /* internal code for tokens like `\.[pausing]' */
	inputCommand      = 31   /* internal code for tokens like `\.[input]' */
	specialTag        = 32   /* internal code for tags that take at most one subscript */
	tag               = 33   /* internal code for nonprimitive tokens */
	digitClass        = 0    /* the class number of \.[0123456789] */
	periodClass       = 1    /* the class number of `\..' */
	spaceClass        = 2    /* the class number of spaces and nonstandard characters */
	percentClass      = 3    /* the class number of `\.\%' */
	stringClass       = 4    /* the class number of `\."' */
	rightParenClass   = 8    /* the class number of `\.)' */
	letterClass       = 9    /* letters and the underline character */
	leftBracketClass  = 17   /* `\.[' */
	rightBracketClass = 18   /* `\.]' */
	invalidClass      = 20   /* bad character in the input */
	endLineClass      = 21   /* end of an input line (\.[MFT] only) */
	maxClass          = 21   /* the largest class number */
	carriageReturn    = 015  /* special code placed in |buffer[limit]| */
	switch1           = 25   /* a label in |get_next| */
	passDigits        = 85   /* another */
	passFraction      = 86   /* and still another, although |goto| is considered harmful */

	// Constants in the outer block
	maxBytes   = 10000 // the number of bytes in tokens; must be less than 65536
	maxNames   = 1000  // number of tokens
	hashSize   = 353   // should be prime
	bufSize    = 100   // maximum length of input line
	lineLength = 79    // lines of \TeX\ output have at most this many characters,
// should be less than 256
)

type (
	// Types in the outer block
	asciiCode = /* 0..255 */ byte // eight-bit numbers

	textFile = knuth.File

	eightBits   = /* 0..255 */ byte     // unsigned one-byte quantity
	sixteenBits = /* 0..65535 */ uint16 // unsigned two-byte quantity

	namePointer = /* 0..maxNames */ uint16 // identifies a name
)

type prg struct {
	stdin, stdout, stderr knuth.File
	/* Globals in the outer block  */ history/* spotless..fatalMessage */ byte // how bad was this run?

	xord [256]asciiCode
	// specifies conversion of input characters
	xchr [256]char
	// specifies conversion of output characters

	mfFile     textFile // primary input
	changeFile textFile // updates
	styleFile  textFile // formatting bootstrap

	texFile textFile

	buffer [101]asciiCode

	line      int32 // the number of the current line in the current file
	otherLine int32 // the number of the current line in the input file that
	//   is not currently being read

	tempLine                   int32 // used when interchanging |line| with |other_line|
	limit/* 0..bufSize */ byte // the last character position occupied in the buffer
	loc/* 0..bufSize */ byte   // the next character position to be read from the buffer
	inputHasEnded              bool // if |true|, there is no more input
	changing                   bool // if |true|, the current line is from |change_file|
	styling                    bool // if |true|, the current line is from |style_file|

	changeBuffer                     [101]asciiCode
	changeLimit/* 0..bufSize */ byte // the last position occupied in |change_buffer|

	byteMem   [10001]asciiCode  // characters of names
	byteStart [1001]sixteenBits // directory into |byte_mem|
	link      [1001]sixteenBits // hash table links
	ilk       [1001]sixteenBits // type codes

	namePtr                         namePointer // first unused position in |byte_start|
	bytePtr/* 0..maxBytes */ uint16 // first unused position in |byte_mem|

	idFirst/* 0..bufSize */ byte // where the current token begins in the buffer
	idLoc/* 0..bufSize */ byte   // just after the current token in the buffer

	hash [354]sixteenBits // heads of hash lists

	translation [256]namePointer
	i           asciiCode // index into |translation|

	trLe, trGe, trNe, trAmp, trSharp, trSkip, trPs,
	trQuad namePointer // special translations

	curType  eightBits // type of token just scanned
	curTok   int32     // hash table or buffer location
	prevType eightBits // previous value of |cur_type|
	prevTok  int32     // previous value of |cur_tok|

	startOfLine bool // has the current line had nothing but spaces so far?
	emptyBuffer bool // is it time to input a new line?

	charClass [256] /* 0..maxClass */ byte // the class numbers

	outBuf                         [80]asciiCode // assembled characters
	outPtr/* 0..lineLength */ byte // number of characters in |out_buf|
	outLine                        int32 // coordinates of next line to be output

	// Error handling procedures
}

func (prg *prg) error1() { // prints `\..' and location of error message
	var (
		k, l /* 0..bufSize */ byte // indices into |buffer|
	)
	{
		if prg.styling {
			prg.stdout.Write(". (style file ")
		} else if prg.changing {
			prg.stdout.Write(". (change file ")
		} else {
			prg.stdout.Write(". (")
		}
		prg.stdout.Writeln("l.", prg.line, knuth.WriteWidth(1), ")")
		if int32(prg.loc) >= int32(prg.limit) {
			l = prg.limit
		} else {
			l = prg.loc
		}
		for ii := int32(1); ii <= int32(l); ii++ {
			k = byte(ii)
			_ = k
			prg.stdout.Write(string(rune(prg.xchr[prg.buffer[int32(k)-1]])))
		} // print the characters already read
		prg.stdout.Writeln()
		for ii := int32(1); ii <= int32(l); ii++ {
			k = byte(ii)
			_ = k
			prg.stdout.Write(" ")
		} // space out the next line
		for ii := int32(l) + 1; ii <= int32(prg.limit); ii++ {
			k = byte(ii)
			_ = k
			prg.stdout.Write(string(rune(prg.xchr[prg.buffer[int32(k)-1]])))
		} // print the part not yet read
	}
	prg.history = byte(errorMessage)
}

func (prg *prg) jumpOut() {
	panic(signal(endOfMft))
}

func (prg *prg) initialize() {
	var (
		// Local variables for initialization
		i/* 0..255 */ byte

		h/* 0..hashSize */ uint16 // index into hash-head array
	)
	prg.history = byte(spotless)

	prg.xchr[040] = ' '
	prg.xchr[041] = '!'
	prg.xchr[042] = '"'
	prg.xchr[043] = '#'
	prg.xchr[044] = '$'
	prg.xchr[045] = '%'
	prg.xchr[046] = '&'
	prg.xchr[047] = '\''

	prg.xchr[050] = '('
	prg.xchr[051] = ')'
	prg.xchr[052] = '*'
	prg.xchr[053] = '+'
	prg.xchr[054] = ','
	prg.xchr[055] = '-'
	prg.xchr[056] = '.'
	prg.xchr[057] = '/'

	prg.xchr[060] = '0'
	prg.xchr[061] = '1'
	prg.xchr[062] = '2'
	prg.xchr[063] = '3'
	prg.xchr[064] = '4'
	prg.xchr[065] = '5'
	prg.xchr[066] = '6'
	prg.xchr[067] = '7'

	prg.xchr[070] = '8'
	prg.xchr[071] = '9'
	prg.xchr[072] = ':'
	prg.xchr[073] = ';'
	prg.xchr[074] = '<'
	prg.xchr[075] = '='
	prg.xchr[076] = '>'
	prg.xchr[077] = '?'

	prg.xchr[0100] = '@'
	prg.xchr[0101] = 'A'
	prg.xchr[0102] = 'B'
	prg.xchr[0103] = 'C'
	prg.xchr[0104] = 'D'
	prg.xchr[0105] = 'E'
	prg.xchr[0106] = 'F'
	prg.xchr[0107] = 'G'

	prg.xchr[0110] = 'H'
	prg.xchr[0111] = 'I'
	prg.xchr[0112] = 'J'
	prg.xchr[0113] = 'K'
	prg.xchr[0114] = 'L'
	prg.xchr[0115] = 'M'
	prg.xchr[0116] = 'N'
	prg.xchr[0117] = 'O'

	prg.xchr[0120] = 'P'
	prg.xchr[0121] = 'Q'
	prg.xchr[0122] = 'R'
	prg.xchr[0123] = 'S'
	prg.xchr[0124] = 'T'
	prg.xchr[0125] = 'U'
	prg.xchr[0126] = 'V'
	prg.xchr[0127] = 'W'

	prg.xchr[0130] = 'X'
	prg.xchr[0131] = 'Y'
	prg.xchr[0132] = 'Z'
	prg.xchr[0133] = '['
	prg.xchr[0134] = '\\'
	prg.xchr[0135] = ']'
	prg.xchr[0136] = '^'
	prg.xchr[0137] = '_'

	prg.xchr[0140] = '`'
	prg.xchr[0141] = 'a'
	prg.xchr[0142] = 'b'
	prg.xchr[0143] = 'c'
	prg.xchr[0144] = 'd'
	prg.xchr[0145] = 'e'
	prg.xchr[0146] = 'f'
	prg.xchr[0147] = 'g'

	prg.xchr[0150] = 'h'
	prg.xchr[0151] = 'i'
	prg.xchr[0152] = 'j'
	prg.xchr[0153] = 'k'
	prg.xchr[0154] = 'l'
	prg.xchr[0155] = 'm'
	prg.xchr[0156] = 'n'
	prg.xchr[0157] = 'o'

	prg.xchr[0160] = 'p'
	prg.xchr[0161] = 'q'
	prg.xchr[0162] = 'r'
	prg.xchr[0163] = 's'
	prg.xchr[0164] = 't'
	prg.xchr[0165] = 'u'
	prg.xchr[0166] = 'v'
	prg.xchr[0167] = 'w'

	prg.xchr[0170] = 'x'
	prg.xchr[0171] = 'y'
	prg.xchr[0172] = 'z'
	prg.xchr[0173] = '{'
	prg.xchr[0174] = '|'
	prg.xchr[0175] = '}'
	prg.xchr[0176] = '~'

	for ii := int32(0); ii <= 037; ii++ {
		i = byte(ii)
		_ = i
		prg.xchr[i] = ' '
	}
	for ii := int32(0177); ii <= 0377; ii++ {
		i = byte(ii)
		_ = i
		prg.xchr[i] = ' '
	}

	for ii := int32(firstTextChar); ii <= lastTextChar; ii++ {
		i = byte(ii)
		_ = i
		prg.xord[i] = 0177
	}
	for ii := int32(0200); ii <= 0377; ii++ {
		i = byte(ii)
		_ = i
		prg.xord[prg.xchr[i]] = i
	}
	for ii := int32(1); ii <= 0176; ii++ {
		i = byte(ii)
		_ = i
		prg.xord[prg.xchr[i]] = i
	}

	prg.texFile.Rewrite()

	prg.byteStart[0] = 0
	prg.bytePtr = 0
	prg.byteStart[1] = 0 // this makes name 0 of length zero
	prg.namePtr = 1

	for ii := int32(0); ii <= hashSize-1; ii++ {
		h = uint16(ii)
		_ = h
		prg.hash[h] = 0
	}

	prg.curType = byte(endOfLine)
	prg.curTok = 0

	for ii := int32('0'); ii <= '9'; ii++ {
		i = byte(ii)
		_ = i
		prg.charClass[i] = byte(digitClass)
	}
	prg.charClass['.'] = byte(periodClass)
	prg.charClass[' '] = byte(spaceClass)
	prg.charClass['%'] = byte(percentClass)
	prg.charClass['"'] = byte(stringClass)

	prg.charClass[','] = 5
	prg.charClass[';'] = 6
	prg.charClass['('] = 7
	prg.charClass[')'] = byte(rightParenClass)
	for ii := int32('A'); ii <= 'Z'; ii++ {
		i = byte(ii)
		_ = i
		prg.charClass[i] = byte(letterClass)
	}
	for ii := int32('a'); ii <= 'z'; ii++ {
		i = byte(ii)
		_ = i
		prg.charClass[i] = byte(letterClass)
	}
	prg.charClass['_'] = byte(letterClass)

	prg.charClass['<'] = 10
	prg.charClass['='] = 10
	prg.charClass['>'] = 10
	prg.charClass[':'] = 10
	prg.charClass['|'] = 10

	prg.charClass['`'] = 11
	prg.charClass['\''] = 11

	prg.charClass['+'] = 12
	prg.charClass['-'] = 12

	prg.charClass['/'] = 13
	prg.charClass['*'] = 13
	prg.charClass['\\'] = 13

	prg.charClass['!'] = 14
	prg.charClass['?'] = 14

	prg.charClass['#'] = 15
	prg.charClass['&'] = 15
	prg.charClass['@'] = 15
	prg.charClass['$'] = 15

	prg.charClass['^'] = 16
	prg.charClass['~'] = 16

	prg.charClass['['] = byte(leftBracketClass)
	prg.charClass[']'] = byte(rightBracketClass)

	prg.charClass['{'] = 19
	prg.charClass['}'] = 19

	for ii := int32(0); ii <= ' '-1; ii++ {
		i = byte(ii)
		_ = i
		prg.charClass[i] = byte(invalidClass)
	}
	prg.charClass[carriageReturn] = byte(endLineClass)

	for ii := int32(127); ii <= 255; ii++ {
		i = byte(ii)
		_ = i
		prg.charClass[i] = byte(invalidClass)
	}

	prg.outPtr = 1
	prg.outBuf[1] = ' '
	prg.outLine = 1
	prg.texFile.Write("\\input mftmac")

	prg.outBuf[0] = '\\'

}

// 5.

// tangle:pos mft.web:164:3:

// Labels are given symbolic names by the following definitions. We insert
// the label `|exit|:' just before the `\ignorespaces|end|\unskip' of a
// procedure in which we have used the `|return|' statement defined below;
// the label `|restart|' is occasionally used at the very beginning of a
// procedure; and the label `|reswitch|' is occasionally used just prior to
// a \&[case] statement in which some cases change the conditions and we wish to
// branch to the newly applicable case.
// Loops that are set up with the \&[loop] construction defined below are
// commonly exited by going to `|done|' or to `|found|' or to `|not_found|',
// and they are sometimes repeated by going to `|continue|'.

// 6.

// tangle:pos mft.web:183:3:

// Here are some macros for common programming idioms.

// 7.

// tangle:pos mft.web:193:3:

// We assume that |case| statements may include a default case that applies
// if no matching label is found. Thus, we shall use constructions like
// \xref[system dependencies]
// $$\vbox[\halign[#\hfil\cr
// |case x of|\cr
// 1: $\langle\,$code for $x=1\,\rangle$;\cr
// 3: $\langle\,$code for $x=3\,\rangle$;\cr
// |othercases| $\langle\,$code for |x<>1| and |x<>3|$\,\rangle$\cr
// |endcases|\cr]]$$
// since most \PASCAL\ compilers have plugged this hole in the language by
// incorporating some sort of default mechanism. For example, the compiler
// used to develop \.[WEB] and \TeX\ allows `|others|:' as a default label,
// and other \PASCAL s allow syntaxes like `\ignorespaces|else|\unskip' or
// `\&[otherwise]' or `\\[otherwise]:', etc. The definitions of |othercases|
// and |endcases| should be changed to agree with local conventions.
// (Of course, if no default mechanism is available, the |case| statements of
// this program must be extended by listing all remaining cases.)

// 11. The character set

// tangle:pos mft.web:249:22:

// \.[MFT] works internally with ASCII codes, like all other programs
// associated with \TeX\ and \MF. The present section has been lifted
// almost verbatim from the \MF\ program.
// \xref[ASCII code]

// 19. Input and output

// tangle:pos mft.web:449:21:

// The I/O conventions of this program are essentially identical to those
// of \.[WEAVE].  Therefore people who need to make modifications should be
// able to do so without too many headaches.

// 22.

// tangle:pos mft.ch:67:3:

// The |update_terminal| procedure is called when we want
// to make sure that everything we have output to the terminal so far has
// actually left the computer's internal buffers and been sent.
// \xref[system dependencies]

// 24.

// tangle:pos mft.web:492:3:

// The following code opens the input files.  Since these files were listed
// in the program header, we assume that the \PASCAL\ runtime system has
// already checked that suitable file names have been given; therefore no
// additional error checking needs to be done.
// \xref[system dependencies]
func (prg *prg) openInput() {
	prg.mfFile.Reset()
	prg.changeFile.Reset()
	prg.styleFile.Reset()
}

// 28.

// tangle:pos mft.web:520:3:

// The |input_ln| procedure brings the next line of input from the specified
// file into the |buffer| array and returns the value |true|, unless the file has
// already been entirely read, in which case it returns |false|. The conventions
// of \TeX\ are followed; i.e., |ASCII_code| numbers representing the next line
// of the file are input into |buffer[0]|, |buffer[1]|, \dots,
// |buffer[limit-1]|; trailing blanks are ignored;
// and the global variable |limit| is set to the length of the
// \xref[system dependencies]
// line. The value of |limit| must be strictly less than |buf_size|.
func (prg *prg) inputLn(f textFile) (r bool) {
	// inputs a line or returns |false|
	var (
		finalLimit /* 0..bufSize */ byte // |limit| without trailing blanks
	)
	prg.limit = 0
	finalLimit = 0
	if f.EOF() {
		r = false
	} else {
		for !f.EOLN() {
			prg.buffer[prg.limit] = prg.xord[*f.ByteP()]
			f.Get()
			prg.limit = byte(int32(prg.limit) + 1)
			if int32(prg.buffer[int32(prg.limit)-1]) != ' ' {
				finalLimit = prg.limit
			}
			if int32(prg.limit) == bufSize {
				for !f.EOLN() {
					f.Get()
				}
				prg.limit = byte(int32(prg.limit) - 1) // keep |buffer[buf_size]| empty
				if int32(finalLimit) > int32(prg.limit) {
					finalLimit = prg.limit
				}
				{
					prg.stdout.Writeln()
					prg.stdout.Write("! Input line too long")
				}
				prg.loc = 0
				prg.error1()
				// \xref[Input line too long]
			}
		}
		f.Readln()
		prg.limit = finalLimit
		r = true
	}
	return r
}

// 32.

// tangle:pos mft.web:610:3:

// Sometimes the program's behavior is far different from what it should be,
// and \.[MFT] prints an error message that is really for the \.[MFT]
// maintenance person, not the user. In such cases the program says
// |confusion('indication of where we are')|.

// 33.

// tangle:pos mft.web:618:3:

// An overflow stop occurs if \.[MFT]'s tables aren't large enough.

// 35.

// tangle:pos mft.web:640:3:

// As we change |changing| from |true| to |false| and back again, we must
// remember to swap the values of |line| and |other_line| so that the |err_print|
// routine will be sure to report the correct line number.

// 37.

// tangle:pos mft.web:658:3:

// Here's a simple function that checks if the two buffers are different.
func (prg *prg) linesDontMatch() (r bool) {
	var (
		k /* 0..bufSize */ byte // index into the buffers
	)
	r = true
	if int32(prg.changeLimit) != int32(prg.limit) {
		goto exit
	}
	if int32(prg.limit) > 0 {
		for ii := int32(0); ii <= int32(prg.limit)-1; ii++ {
			k = byte(ii)
			_ = k
			if int32(prg.changeBuffer[k]) != int32(prg.buffer[k]) {
				goto exit
			}
		}
	}
	r = false

exit:
	;
	return r
}

// 38.

// tangle:pos mft.web:670:3:

// Procedure |prime_the_change_buffer| sets |change_buffer| in preparation
// for the next matching operation. Since blank lines in the change file are
// not used for matching, we have |(change_limit=0)and not changing| if and
// only if the change file is exhausted. This procedure is called only
// when |changing| is true; hence error messages will be reported correctly.
func (prg *prg) primeTheChangeBuffer() {
	var (
		k /* 0..bufSize */ byte // index into the buffers
	)
	prg.changeLimit = 0 // this value will be used if the change file ends

	// Skip over comment lines in the change file; |return| if end of file
	for true {
		prg.line = prg.line + 1
		if !prg.inputLn(prg.changeFile) {
			goto exit
		}
		if int32(prg.limit) < 2 {
			goto continue1
		}
		if int32(prg.buffer[0]) != '@' {
			goto continue1
		}
		if int32(prg.buffer[1]) >= 'X' && int32(prg.buffer[1]) <= 'Z' {
			prg.buffer[1] = byte(int32(prg.buffer[1]) + 'z' - 'Z')
		} // lowercasify
		if int32(prg.buffer[1]) == 'x' {
			goto done
		}
		if int32(prg.buffer[1]) == 'y' || int32(prg.buffer[1]) == 'z' {
			prg.loc = 2
			{
				prg.stdout.Writeln()
				prg.stdout.Write("! Where is the matching @x?")
				prg.error1()
			}
			// \xref[Where is the match...]
		}

	continue1:
	}

done:
	;

	// Skip to the next nonblank line; |return| if end of file
	for {
		prg.line = prg.line + 1
		if !prg.inputLn(prg.changeFile) {
			{
				prg.stdout.Writeln()
				prg.stdout.Write("! Change file ended after @x")
				prg.error1()
			}
			// \xref[Change file ended...]

			// \xref[Change file ended...]
			goto exit
		}
		if int32(prg.limit) > 0 {
			break
		}
	}

	// Move |buffer| and |limit| to |change_buffer| and |change_limit|
	{
		prg.changeLimit = prg.limit
		if int32(prg.limit) > 0 {
			for ii := int32(0); ii <= int32(prg.limit)-1; ii++ {
				k = byte(ii)
				_ = k
				prg.changeBuffer[k] = prg.buffer[k]
			}
		}
	}

exit:
}

// 42.

// tangle:pos mft.web:721:3:

// The following procedure is used to see if the next change entry should
// go into effect; it is called only when |changing| is false.
// The idea is to test whether or not the current
// contents of |buffer| matches the current contents of |change_buffer|.
// If not, there's nothing more to do; but if so, a change is called for:
// All of the text down to the \.[@y] is supposed to match. An error
// message is issued if any discrepancy is found. Then the procedure
// prepares to read the next line from |change_file|.
func (prg *prg) checkChange() {
	var (
		n                      int32 // the number of discrepancies found
		k/* 0..bufSize */ byte       // index into the buffers
	)
	if prg.linesDontMatch() {
		goto exit
	}
	n = 0
	for true {
		prg.changing = !prg.changing
		prg.tempLine = prg.otherLine
		prg.otherLine = prg.line
		prg.line = prg.tempLine // now it's |true|
		prg.line = prg.line + 1
		if !prg.inputLn(prg.changeFile) {
			{
				prg.stdout.Writeln()
				prg.stdout.Write("! Change file ended before @y")
				prg.error1()
			}
			// \xref[Change file ended...]
			prg.changeLimit = 0
			prg.changing = !prg.changing
			prg.tempLine = prg.otherLine
			prg.otherLine = prg.line
			prg.line = prg.tempLine // |false| again
			// |false| again
			goto exit
		}

		// If the current line starts with \.[@y], report any discrepancies and |return|
		if int32(prg.limit) > 1 {
			if int32(prg.buffer[0]) == '@' {
				if int32(prg.buffer[1]) >= 'X' && int32(prg.buffer[1]) <= 'Z' {
					prg.buffer[1] = byte(int32(prg.buffer[1]) + 'z' - 'Z')
				} // lowercasify
				if int32(prg.buffer[1]) == 'x' || int32(prg.buffer[1]) == 'z' {
					prg.loc = 2
					{
						prg.stdout.Writeln()
						prg.stdout.Write("! Where is the matching @y?")
						prg.error1()
					}
					// \xref[Where is the match...]
				} else if int32(prg.buffer[1]) == 'y' {
					if n > 0 {
						prg.loc = 2
						{
							prg.stdout.Writeln()
							prg.stdout.Write("! Hmm... ", n, knuth.WriteWidth(1),
								" of the preceding lines failed to match")
							prg.error1()
						}
						// \xref[Hmm... n of the preceding...]
					}

					goto exit
				}
			}
		}

		// Move |buffer| and |limit|...
		{
			prg.changeLimit = prg.limit
			if int32(prg.limit) > 0 {
				for ii := int32(0); ii <= int32(prg.limit)-1; ii++ {
					k = byte(ii)
					_ = k
					prg.changeBuffer[k] = prg.buffer[k]
				}
			}
		}
		prg.changing = !prg.changing
		prg.tempLine = prg.otherLine
		prg.otherLine = prg.line
		prg.line = prg.tempLine // now it's |false|
		prg.line = prg.line + 1
		if !prg.inputLn(prg.mfFile) {
			{
				prg.stdout.Writeln()
				prg.stdout.Write("! MF file ended during a change")
				prg.error1()
			}
			// \xref[MF file ended...]
			prg.inputHasEnded = true
			goto exit
		}
		if prg.linesDontMatch() {
			n = n + 1
		}
	}

exit:
}

// 45.

// tangle:pos mft.web:784:3:

// The |get_line| procedure is called when |loc>limit|; it puts the next
// line of merged input into the buffer and updates the other variables
// appropriately.
func (prg *prg) getLine() {
restart:
	if prg.styling {
		prg.line = prg.line + 1
		if !prg.inputLn(prg.styleFile) {
			prg.styling = false
			prg.line = 0
		}
	}
	if !prg.styling {
		if prg.changing {
			prg.line = prg.line + 1
			if !prg.inputLn(prg.changeFile) {
				{
					prg.stdout.Writeln()
					prg.stdout.Write("! Change file ended without @z")
					prg.error1()
				}
				// \xref[Change file ended...]
				prg.buffer[0] = '@'
				prg.buffer[1] = 'z'
				prg.limit = 2
			}
			if int32(prg.limit) > 1 {
				if int32(prg.buffer[0]) == '@' {
					if int32(prg.buffer[1]) >= 'X' && int32(prg.buffer[1]) <= 'Z' {
						prg.buffer[1] = byte(int32(prg.buffer[1]) + 'z' - 'Z')
					} // lowercasify
					if int32(prg.buffer[1]) == 'x' || int32(prg.buffer[1]) == 'y' {
						prg.loc = 2
						{
							prg.stdout.Writeln()
							prg.stdout.Write("! Where is the matching @z?")
							prg.error1()
						}
						// \xref[Where is the match...]
					} else if int32(prg.buffer[1]) == 'z' {
						prg.primeTheChangeBuffer()
						prg.changing = !prg.changing
						prg.tempLine = prg.otherLine
						prg.otherLine = prg.line
						prg.line = prg.tempLine
					}
				}
			}
		}
		if !prg.changing {
			{
				prg.line = prg.line + 1
				if !prg.inputLn(prg.mfFile) {
					prg.inputHasEnded = true
				} else if int32(prg.changeLimit) > 0 {
					prg.checkChange()
				}
			}
			if prg.changing {
				goto restart
			}
		}
	}
}

// 58.

// tangle:pos mft.web:920:3:

// Here now is the main procedure for finding tokens.
func (prg *prg) lookup() (r namePointer) {
	var (
		i/* 0..bufSize */ byte                // index into |buffer|
		h/* 0..hashSize */ uint16             // hash code
		k/* 0..maxBytes */ uint16             // index into |byte_mem|
		l/* 0..bufSize */ byte                // length of the given token
		p                         namePointer // where the token is being sought
	)
	l = byte(int32(prg.idLoc) - int32(prg.idFirst)) // compute the length

	// Compute the hash code |h|
	h = uint16(prg.buffer[prg.idFirst])
	i = byte(int32(prg.idFirst) + 1)
	for int32(i) < int32(prg.idLoc) {
		h = uint16((int32(h) + int32(h) + int32(prg.buffer[i])) % hashSize)
		i = byte(int32(i) + 1)
	}

	// Compute the name location |p|
	p = prg.hash[h]
	for int32(p) != 0 {
		if int32(prg.byteStart[int32(p)+1])-int32(prg.byteStart[p]) == int32(l) {
			i = prg.idFirst
			k = prg.byteStart[p]
			for int32(i) < int32(prg.idLoc) && int32(prg.buffer[i]) == int32(prg.byteMem[k]) {
				i = byte(int32(i) + 1)
				k = uint16(int32(k) + 1)
			}
			if int32(i) == int32(prg.idLoc) {
				goto found
			} // all characters agree
		}
		p = prg.link[p]
	}
	p = prg.namePtr // the current token is new
	prg.link[p] = prg.hash[h]
	prg.hash[h] = p // insert |p| at beginning of hash list
	// insert |p| at beginning of hash list
found:
	;
	if int32(p) == int32(prg.namePtr) {
		if int32(prg.bytePtr)+int32(l) > maxBytes {
			prg.stdout.Writeln()
			prg.stdout.Write("! Sorry, ", "byte memory", " capacity exceeded")
			prg.error1()
			prg.history = byte(fatalMessage)
			prg.jumpOut()
		}
		if int32(prg.namePtr)+1 > maxNames {
			prg.stdout.Writeln()
			prg.stdout.Write("! Sorry, ", "name", " capacity exceeded")
			prg.error1()
			prg.history = byte(fatalMessage)
			prg.jumpOut()
		}
		i = prg.idFirst // get ready to move the token into |byte_mem|
		for int32(i) < int32(prg.idLoc) {
			prg.byteMem[prg.bytePtr] = prg.buffer[i]
			prg.bytePtr = uint16(int32(prg.bytePtr) + 1)
			i = byte(int32(i) + 1)
		}
		prg.namePtr = uint16(int32(prg.namePtr) + 1)
		prg.byteStart[prg.namePtr] = prg.bytePtr

		// Assign the default value to |ilk[p]|
		prg.ilk[p] = uint16(tag)
	}
	r = p
	return r
}

// 64.

// tangle:pos mft.web:1026:3:

// We have to get \MF's primitives into the hash table, and the
// simplest way to do this is to insert them every time \.[MFT] is run.
//
// A few macros permit us to do the initialization with a compact program.
// We use the fact that the longest primitive is \.[intersectiontimes],
// which is 17 letters long.

// 80.

// tangle:pos mft.web:1489:3:

// And now we're ready to take the plunge into |get_next| itself.
func (prg *prg) getNext() {
	var (
		c     asciiCode // the current character in the buffer
		class asciiCode // its class number
	)
	prg.prevType = prg.curType
	prg.prevTok = prg.curTok
	if prg.emptyBuffer {
		prg.getLine()
		if prg.inputHasEnded {
			prg.curType = byte(endOfFile)
			prg.curTok = int32(prg.idFirst)
			goto exit
		}
		prg.buffer[prg.limit] = byte(carriageReturn)
		prg.loc = 0
		prg.startOfLine = true
		prg.emptyBuffer = false
	}

switch1:
	c = prg.buffer[prg.loc]
	prg.idFirst = prg.loc
	prg.loc = byte(int32(prg.loc) + 1)
	class = prg.charClass[c]

	// Branch on the |class|, scan the token; |return| directly if the token is special, or |goto found| if it needs to be looked up
	switch class {
	case digitClass:
		goto passDigits
	case periodClass:
		class = prg.charClass[prg.buffer[prg.loc]]
		if int32(class) > periodClass {
			goto switch1
		} else if int32(class) < periodClass {
			goto passFraction
		} // |class=digit_class|

	case spaceClass:
		if prg.startOfLine {
			prg.curType = byte(indentation)
			prg.curTok = int32(prg.idFirst)
			goto exit
		} else {
			goto switch1
		}
	case endLineClass: /*   */
		prg.curType = byte(endOfLine)
		prg.curTok = int32(prg.idFirst)
		goto exit
	case stringClass:
		// Get a string token and |return|
		for true {
			if int32(prg.buffer[prg.loc]) == '"' {
				prg.loc = byte(int32(prg.loc) + 1) /*   */
				{
					prg.curType = byte(stringToken)
					prg.curTok = int32(prg.idFirst)
					goto exit
				}
			}
			if int32(prg.loc) == int32(prg.limit) {
				{
					prg.stdout.Writeln()
					prg.stdout.Write("! Incomplete string will be ignored")
					prg.error1()
				}
				goto switch1
				// \xref[Incomplete string...]
			}
			prg.loc = byte(int32(prg.loc) + 1)
		}

	case 5, 6, 7, 8:
		goto found
	case invalidClass:
		// Decry the invalid character and |goto switch|
		{
			prg.stdout.Writeln()
			prg.stdout.Write("! Invalid character will be ignored")
			prg.error1()
		}
		goto switch1
	// \xref[Invalid character...]

	default: // letters, etc.
	}

	for int32(prg.charClass[prg.buffer[prg.loc]]) == int32(class) {
		prg.loc = byte(int32(prg.loc) + 1)
	}

	goto found

passDigits:
	for int32(prg.charClass[prg.buffer[prg.loc]]) == digitClass {
		prg.loc = byte(int32(prg.loc) + 1)
	}
	if int32(prg.buffer[prg.loc]) != '.' {
		goto done
	}
	if int32(prg.charClass[prg.buffer[int32(prg.loc)+1]]) != digitClass {
		goto done
	}
	prg.loc = byte(int32(prg.loc) + 1)

passFraction:
	for {
		prg.loc = byte(int32(prg.loc) + 1)
		if int32(prg.charClass[prg.buffer[prg.loc]]) != digitClass {
			break
		}
	}

done:
	{
		prg.curType = byte(numericToken)
		prg.curTok = int32(prg.idFirst)
		goto exit
	}

found:
	prg.idLoc = prg.loc
	prg.curTok = int32(prg.lookup())
	prg.curType = byte(prg.ilk[prg.curTok])

exit:
}

// 87.

// tangle:pos mft.web:1571:3:

// The |flush_buffer| routine empties the buffer up to a given breakpoint,
// and moves any remaining characters to the beginning of the next line.
// If the |per_cent| parameter is |true|, a |"%"| is appended to the line
// that is being output; in this case the breakpoint |b| should be strictly
// less than |line_length|. If the |per_cent| parameter is |false|,
// trailing blanks are suppressed.
// The characters emptied from the buffer form a new line of output.
func (prg *prg) flushBuffer(b eightBits, perCent bool) {
	var (
		j, k /* 0..lineLength */ byte
	)
	j = b
	if !perCent {
		for true {
			if int32(j) == 0 {
				goto done
			}
			if int32(prg.outBuf[j]) != ' ' {
				goto done
			}
			j = byte(int32(j) - 1)
		}
	}

done:
	for ii := int32(1); ii <= int32(j); ii++ {
		k = byte(ii)
		_ = k
		prg.texFile.Write(string(rune(prg.xchr[prg.outBuf[k]])))
	}
	if perCent {
		prg.texFile.Write(string(rune(prg.xchr['%'])))
	}
	prg.texFile.Writeln()
	prg.outLine = prg.outLine + 1
	if int32(b) < int32(prg.outPtr) {
		for ii := int32(b) + 1; ii <= int32(prg.outPtr); ii++ {
			k = byte(ii)
			_ = k
			prg.outBuf[int32(k)-int32(b)] = prg.outBuf[k]
		}
	}
	prg.outPtr = byte(int32(prg.outPtr) - int32(b))
}

// 89.

// tangle:pos mft.web:1605:3:

// When we wish to append the character |c| to the output buffer, we write
// `$|out|(c)$'; this will cause the buffer to be emptied if it was already
// full. Similarly, `$|out2|(c_1)(c_2)$' appends a pair of characters.
// A line break will occur at a space or after a single-nonletter
// \TeX\ control sequence.

// 91.

// tangle:pos mft.web:1632:3:

// A long line is broken at a blank space or just before a backslash that isn't
// preceded by another backslash. In the latter case, a |"%"| is output at
// the break. (This policy has a known bug, in the rare situation that the
// backslash was in a string constant that's being output “verbatim.”)
func (prg *prg) breakOut() {
	var (
		k/* 0..lineLength */ byte           // index into |out_buf|
		d                         asciiCode // character from the buffer
	)
	k = prg.outPtr
	for true {
		if int32(k) == 0 {
			{
				prg.stdout.Writeln()
				prg.stdout.Write("! Line had to be broken (output l.", prg.outLine, knuth.WriteWidth(1))
			}
			// \xref[Line had to be broken]
			prg.stdout.Writeln("):")
			for ii := int32(1); ii <= int32(prg.outPtr)-1; ii++ {
				k = byte(ii)
				_ = k
				prg.stdout.Write(string(rune(prg.xchr[prg.outBuf[k]])))
			}
			prg.stdout.Writeln() /*   */
			if int32(prg.history) == spotless {
				prg.history = byte(harmlessMessage)
			}
			prg.flushBuffer(eightBits(int32(prg.outPtr)-1), true)
			goto exit
		}
		d = prg.outBuf[k]
		if int32(d) == ' ' {
			prg.flushBuffer(k, false)
			goto exit
		}
		if int32(d) == '\\' && int32(prg.outBuf[int32(k)-1]) != '\\' {
			prg.flushBuffer(eightBits(int32(k)-1), true)
			goto exit
		}
		k = byte(int32(k) - 1)
	}

exit:
}

// 93.

// tangle:pos mft.web:1669:3:

// To output a string of bytes from |byte_mem|, we call |out_str|.
func (prg *prg) outStr(p namePointer) { // outputs a string
	var (
		k /* 0..maxBytes */ uint16 // index into |byte_mem|
	)
	for ii := int32(prg.byteStart[p]); ii <= int32(prg.byteStart[int32(p)+1])-1; ii++ {
		k = uint16(ii)
		_ = k
		if int32(prg.outPtr) == lineLength {
			prg.breakOut()
		}
		prg.outPtr = byte(int32(prg.outPtr) + 1)
		prg.outBuf[prg.outPtr] = prg.byteMem[k]
	}
}

// 94.

// tangle:pos mft.web:1676:3:

// The |out_name| subroutine is used to output a symbolic token.
// Unusual characters are translated into forms that won't screw up.
func (prg *prg) outName(p namePointer) { // outputs a name
	var (
		k/* 0..maxBytes */ uint16             // index into |byte_mem|
		t                         namePointer // translation of character being output, if any
	)
	for ii := int32(prg.byteStart[p]); ii <= int32(prg.byteStart[int32(p)+1])-1; ii++ {
		k = uint16(ii)
		_ = k
		t = prg.translation[prg.byteMem[k]]
		if int32(t) == 0 {
			if int32(prg.outPtr) == lineLength {
				prg.breakOut()
			}
			prg.outPtr = byte(int32(prg.outPtr) + 1)
			prg.outBuf[prg.outPtr] = prg.byteMem[k]
		} else {
			prg.outStr(t)
		}
	}
}

// 95.

// tangle:pos mft.web:1689:3:

// We often want to output a name after calling a numeric macro
// (e.g., `\.[\\1\[foo\]]').
func (prg *prg) outMacAndName(n asciiCode, p namePointer) {
	{
		if int32(prg.outPtr) == lineLength {
			prg.breakOut()
		}
		prg.outPtr = byte(int32(prg.outPtr) + 1)
		prg.outBuf[prg.outPtr] = '\\'
	}
	{
		if int32(prg.outPtr) == lineLength {
			prg.breakOut()
		}
		prg.outPtr = byte(int32(prg.outPtr) + 1)
		prg.outBuf[prg.outPtr] = n
	}
	if int32(prg.byteStart[int32(p)+1])-int32(prg.byteStart[p]) == 1 {
		prg.outName(p)
	} else {
		{
			if int32(prg.outPtr) == lineLength {
				prg.breakOut()
			}
			prg.outPtr = byte(int32(prg.outPtr) + 1)
			prg.outBuf[prg.outPtr] = '{'
		}
		prg.outName(p)
		{
			if int32(prg.outPtr) == lineLength {
				prg.breakOut()
			}
			prg.outPtr = byte(int32(prg.outPtr) + 1)
			prg.outBuf[prg.outPtr] = '}'
		}
	}
}

// 96.

// tangle:pos mft.web:1699:3:

// Here's a routine that simply copies from the input buffer to the output
// buffer.
func (prg *prg) copy(firstLoc int32) { // output |buffer[first_loc..loc-1]|
	var (
		k /* 0..bufSize */ byte // |buffer| location being copied
	)
	for ii := firstLoc; ii <= int32(prg.loc)-1; ii++ {
		k = byte(ii)
		_ = k
		if int32(prg.outPtr) == lineLength {
			prg.breakOut()
		}
		prg.outPtr = byte(int32(prg.outPtr) + 1)
		prg.outBuf[prg.outPtr] = prg.buffer[k]
	}
}

// 97. Translation

// tangle:pos mft.web:1707:16:

// The main work of \.[MFT] is accomplished by a routine that translates
// the tokens, one by one, with a limited amount of lookahead/lookbehind.
// Automata theorists might loosely call this a “finite state transducer,”
// because the flow of control is comparatively simple.
func (prg *prg) doTheTranslation() {
	var (
		k/* 0..bufSize */ byte       // looks ahead in the buffer
		t                      int32 // type that spreads to new tokens
	)
restart:
	if int32(prg.outPtr) > 0 {
		prg.flushBuffer(prg.outPtr, false)
	}
	prg.emptyBuffer = true
	for true {
		prg.getNext()
		if prg.startOfLine {
			if int32(prg.curType) >= minActionType {
				{
					if int32(prg.outPtr) == lineLength {
						prg.breakOut()
					}
					prg.outPtr = byte(int32(prg.outPtr) + 1)
					prg.outBuf[prg.outPtr] = '$'
				}
				prg.startOfLine = false
				switch prg.curType {
				case endit:
					if int32(prg.outPtr) == lineLength {
						prg.breakOut()
					}
					prg.outPtr = byte(int32(prg.outPtr) + 1)
					prg.outBuf[prg.outPtr] = '\\'
					if int32(prg.outPtr) == lineLength {
						prg.breakOut()
					}
					prg.outPtr = byte(int32(prg.outPtr) + 1)
					prg.outBuf[prg.outPtr] = '!'
				// \xref[\\!]
				case binary, abinary, bbinary, ampersand,
					pythSub:
					if int32(prg.outPtr) == lineLength {
						prg.breakOut()
					}
					prg.outPtr = byte(int32(prg.outPtr) + 1)
					prg.outBuf[prg.outPtr] = '{'
					if int32(prg.outPtr) == lineLength {
						prg.breakOut()
					}
					prg.outPtr = byte(int32(prg.outPtr) + 1)
					prg.outBuf[prg.outPtr] = '}'

				// \xref[\[\]]
				default:
				}
			} else if int32(prg.curType) == endOfLine {
				prg.outStr(prg.trSkip)
				goto restart
			} else if int32(prg.curType) == mftComment {
				goto restart
			}
		}

	reswitch:
		switch prg.curType {
		case numericToken:
			// Translate a numeric token or a fraction
			if int32(prg.buffer[prg.loc]) == '/' {
				if int32(prg.charClass[prg.buffer[int32(prg.loc)+1]]) == digitClass {
					{
						if int32(prg.outPtr) == lineLength {
							prg.breakOut()
						}
						prg.outPtr = byte(int32(prg.outPtr) + 1)
						prg.outBuf[prg.outPtr] = '\\'
						if int32(prg.outPtr) == lineLength {
							prg.breakOut()
						}
						prg.outPtr = byte(int32(prg.outPtr) + 1)
						prg.outBuf[prg.outPtr] = 'f'
						if int32(prg.outPtr) == lineLength {
							prg.breakOut()
						}
						prg.outPtr = byte(int32(prg.outPtr) + 1)
						prg.outBuf[prg.outPtr] = 'r'
						if int32(prg.outPtr) == lineLength {
							prg.breakOut()
						}
						prg.outPtr = byte(int32(prg.outPtr) + 1)
						prg.outBuf[prg.outPtr] = 'a'
						if int32(prg.outPtr) == lineLength {
							prg.breakOut()
						}
						prg.outPtr = byte(int32(prg.outPtr) + 1)
						prg.outBuf[prg.outPtr] = 'c'
					}
					prg.copy(prg.curTok)
					prg.getNext()
					// \xref[\\frac]
					{
						if int32(prg.outPtr) == lineLength {
							prg.breakOut()
						}
						prg.outPtr = byte(int32(prg.outPtr) + 1)
						prg.outBuf[prg.outPtr] = '/'
						if int32(prg.outPtr) == lineLength {
							prg.breakOut()
						}
						prg.outPtr = byte(int32(prg.outPtr) + 1)
						prg.outBuf[prg.outPtr] = '{'
					}
					prg.getNext()
					prg.copy(prg.curTok)
					{
						if int32(prg.outPtr) == lineLength {
							prg.breakOut()
						}
						prg.outPtr = byte(int32(prg.outPtr) + 1)
						prg.outBuf[prg.outPtr] = '}'
					}
				} else {
					prg.copy(prg.curTok)
				}
			} else {
				prg.copy(prg.curTok)
			}

		case stringToken:
			// Translate a string token
			{
				if int32(prg.outPtr) == lineLength {
					prg.breakOut()
				}
				prg.outPtr = byte(int32(prg.outPtr) + 1)
				prg.outBuf[prg.outPtr] = '\\'
				if int32(prg.outPtr) == lineLength {
					prg.breakOut()
				}
				prg.outPtr = byte(int32(prg.outPtr) + 1)
				prg.outBuf[prg.outPtr] = '7'
			}
			prg.copy(prg.curTok)
		// \xref[\\7]

		case indentation:
			prg.outStr(prg.trQuad)
		case endOfLine, mftComment:
			// Wind up a line of translation and |goto restart|, or finish a \pb\ segment and |goto reswitch|
			{
				if int32(prg.outPtr) == lineLength {
					prg.breakOut()
				}
				prg.outPtr = byte(int32(prg.outPtr) + 1)
				prg.outBuf[prg.outPtr] = '$'
			}
			if int32(prg.loc) < int32(prg.limit) && int32(prg.curType) == endOfLine {
				prg.curType = byte(recomment)
				goto reswitch
			} else {
				{
					if int32(prg.outPtr) == lineLength {
						prg.breakOut()
					}
					prg.outPtr = byte(int32(prg.outPtr) + 1)
					prg.outBuf[prg.outPtr] = '\\'
					if int32(prg.outPtr) == lineLength {
						prg.breakOut()
					}
					prg.outPtr = byte(int32(prg.outPtr) + 1)
					prg.outBuf[prg.outPtr] = 'p'
					if int32(prg.outPtr) == lineLength {
						prg.breakOut()
					}
					prg.outPtr = byte(int32(prg.outPtr) + 1)
					prg.outBuf[prg.outPtr] = 'a'
					if int32(prg.outPtr) == lineLength {
						prg.breakOut()
					}
					prg.outPtr = byte(int32(prg.outPtr) + 1)
					prg.outBuf[prg.outPtr] = 'r'
				}
				goto restart
				// \xref[\\par]
			}

		case endOfFile:
			goto exit
		// \4
		// Cases that translate primitive tokens
		case op:
			prg.outMacAndName(asciiCode('1'), namePointer(prg.curTok))
		// \xref[\\1]
		case command:
			prg.outMacAndName(asciiCode('2'), namePointer(prg.curTok))
		// \xref[\\2]
		case typeName:
			if int32(prg.prevType) == command {
				prg.outMacAndName(asciiCode('1'), namePointer(prg.curTok))
			} else {
				prg.outMacAndName(asciiCode('2'), namePointer(prg.curTok))
			}
		case endit:
			prg.outMacAndName(asciiCode('3'), namePointer(prg.curTok))
		// \xref[\\3]
		case bbinary:
			prg.outMacAndName(asciiCode('4'), namePointer(prg.curTok))
		// \xref[\\4]
		case bold:
			prg.outMacAndName(asciiCode('5'), namePointer(prg.curTok))
		// \xref[\\5]
		case binary:
			prg.outMacAndName(asciiCode('6'), namePointer(prg.curTok))
		// \xref[\\6]
		case pathJoin:
			prg.outMacAndName(asciiCode('8'), namePointer(prg.curTok))
		// \xref[\\8]
		case colon:
			prg.outMacAndName(asciiCode('?'), namePointer(prg.curTok))
		// \xref[\\?]

		case asIs, sharp, abinary:
			prg.outName(namePointer(prg.curTok))
		case doubleBack:
			if int32(prg.outPtr) == lineLength {
				prg.breakOut()
			}
			prg.outPtr = byte(int32(prg.outPtr) + 1)
			prg.outBuf[prg.outPtr] = '\\'
			if int32(prg.outPtr) == lineLength {
				prg.breakOut()
			}
			prg.outPtr = byte(int32(prg.outPtr) + 1)
			prg.outBuf[prg.outPtr] = ';'
		// \xref[\\;]
		case semicolon:
			prg.outName(namePointer(prg.curTok))
			prg.getNext()
			if int32(prg.curType) != endOfLine {
				if int32(prg.curType) != endit {
					if int32(prg.outPtr) == lineLength {
						prg.breakOut()
					}
					prg.outPtr = byte(int32(prg.outPtr) + 1)
					prg.outBuf[prg.outPtr] = '\\'
					if int32(prg.outPtr) == lineLength {
						prg.breakOut()
					}
					prg.outPtr = byte(int32(prg.outPtr) + 1)
					prg.outBuf[prg.outPtr] = ' '
				}
			}
			// \xref[\\\char32]

			// \xref[\\\char32]
			goto reswitch

		case backslash:
			prg.outStr(prg.translation['\\'])
		case pythSub:
			prg.outStr(prg.trPs)
		case lessOrEqual:
			prg.outStr(prg.trLe)
		case greaterOrEqual:
			prg.outStr(prg.trGe)
		case notEqual:
			prg.outStr(prg.trNe)
		case ampersand:
			prg.outStr(prg.trAmp)

		case inputCommand:
			prg.outMacAndName(asciiCode('2'), namePointer(prg.curTok))
			{
				if int32(prg.outPtr) == lineLength {
					prg.breakOut()
				}
				prg.outPtr = byte(int32(prg.outPtr) + 1)
				prg.outBuf[prg.outPtr] = '\\'
				if int32(prg.outPtr) == lineLength {
					prg.breakOut()
				}
				prg.outPtr = byte(int32(prg.outPtr) + 1)
				prg.outBuf[prg.outPtr] = 'h'
				if int32(prg.outPtr) == lineLength {
					prg.breakOut()
				}
				prg.outPtr = byte(int32(prg.outPtr) + 1)
				prg.outBuf[prg.outPtr] = 'b'
				if int32(prg.outPtr) == lineLength {
					prg.breakOut()
				}
				prg.outPtr = byte(int32(prg.outPtr) + 1)
				prg.outBuf[prg.outPtr] = 'o'
				if int32(prg.outPtr) == lineLength {
					prg.breakOut()
				}
				prg.outPtr = byte(int32(prg.outPtr) + 1)
				prg.outBuf[prg.outPtr] = 'x'
			}

			// Scan the file name and output it in \.[typewriter type]
			for int32(prg.buffer[prg.loc]) == ' ' {
				prg.loc = byte(int32(prg.loc) + 1)
			}
			{
				if int32(prg.outPtr) == lineLength {
					prg.breakOut()
				}
				prg.outPtr = byte(int32(prg.outPtr) + 1)
				prg.outBuf[prg.outPtr] = '{'
				if int32(prg.outPtr) == lineLength {
					prg.breakOut()
				}
				prg.outPtr = byte(int32(prg.outPtr) + 1)
				prg.outBuf[prg.outPtr] = '\\'
				if int32(prg.outPtr) == lineLength {
					prg.breakOut()
				}
				prg.outPtr = byte(int32(prg.outPtr) + 1)
				prg.outBuf[prg.outPtr] = 't'
				if int32(prg.outPtr) == lineLength {
					prg.breakOut()
				}
				prg.outPtr = byte(int32(prg.outPtr) + 1)
				prg.outBuf[prg.outPtr] = 't'
				if int32(prg.outPtr) == lineLength {
					prg.breakOut()
				}
				prg.outPtr = byte(int32(prg.outPtr) + 1)
				prg.outBuf[prg.outPtr] = ' '
			}
			for int32(prg.buffer[prg.loc]) != ' ' && int32(prg.buffer[prg.loc]) != '%' && int32(prg.buffer[prg.loc]) != ';' && int32(prg.loc) < int32(prg.limit) {
				{
					if int32(prg.outPtr) == lineLength {
						prg.breakOut()
					}
					prg.outPtr = byte(int32(prg.outPtr) + 1)
					prg.outBuf[prg.outPtr] = prg.buffer[prg.loc]
				}
				prg.loc = byte(int32(prg.loc) + 1)
			}
			{
				if int32(prg.outPtr) == lineLength {
					prg.breakOut()
				}
				prg.outPtr = byte(int32(prg.outPtr) + 1)
				prg.outBuf[prg.outPtr] = '}'
			}

		case comment, recomment:
			// Translate a comment and |goto restart|, unless there's a \pb\ segment
			if int32(prg.curType) == comment {
				if int32(prg.outPtr) == lineLength {
					prg.breakOut()
				}
				prg.outPtr = byte(int32(prg.outPtr) + 1)
				prg.outBuf[prg.outPtr] = '\\'
				if int32(prg.outPtr) == lineLength {
					prg.breakOut()
				}
				prg.outPtr = byte(int32(prg.outPtr) + 1)
				prg.outBuf[prg.outPtr] = '9'
			}
			// \xref[\\9]
			prg.idFirst = prg.loc
			for int32(prg.loc) < int32(prg.limit) && int32(prg.buffer[prg.loc]) != '|' {
				prg.loc = byte(int32(prg.loc) + 1)
			}
			prg.copy(int32(prg.idFirst))
			if int32(prg.loc) < int32(prg.limit) {
				prg.startOfLine = true
				prg.loc = byte(int32(prg.loc) + 1)
				k = prg.loc
				for int32(k) < int32(prg.limit) && int32(prg.buffer[k]) != '|' {
					k = byte(int32(k) + 1)
				}
				prg.buffer[k] = byte(carriageReturn)
			} else {
				if int32(prg.outBuf[prg.outPtr]) == '\\' {
					if int32(prg.outPtr) == lineLength {
						prg.breakOut()
					}
					prg.outPtr = byte(int32(prg.outPtr) + 1)
					prg.outBuf[prg.outPtr] = ' '
				}
				{
					if int32(prg.outPtr) == lineLength {
						prg.breakOut()
					}
					prg.outPtr = byte(int32(prg.outPtr) + 1)
					prg.outBuf[prg.outPtr] = '\\'
					if int32(prg.outPtr) == lineLength {
						prg.breakOut()
					}
					prg.outPtr = byte(int32(prg.outPtr) + 1)
					prg.outBuf[prg.outPtr] = 'p'
					if int32(prg.outPtr) == lineLength {
						prg.breakOut()
					}
					prg.outPtr = byte(int32(prg.outPtr) + 1)
					prg.outBuf[prg.outPtr] = 'a'
					if int32(prg.outPtr) == lineLength {
						prg.breakOut()
					}
					prg.outPtr = byte(int32(prg.outPtr) + 1)
					prg.outBuf[prg.outPtr] = 'r'
				}
				goto restart
				// \xref[\\par]
			}

		case verbatim:
			// Copy the rest of the current input line to the output, then |goto restart|
			prg.idFirst = prg.loc
			prg.loc = prg.limit
			prg.copy(int32(prg.idFirst))
			if int32(prg.outPtr) == 0 {
				prg.outPtr = 1
				prg.outBuf[1] = ' '
			}

			goto restart

		case setFormat:
			// Change the translation format of tokens, and |goto restart| or |reswitch|
			prg.startOfLine = false
			prg.getNext()
			t = int32(prg.curType)
			for int32(prg.curType) >= minSymbolicToken {
				prg.getNext()
				if int32(prg.curType) >= minSymbolicToken {
					prg.ilk[prg.curTok] = uint16(t)
				}
			}
			if int32(prg.curType) != endOfLine {
				if int32(prg.curType) != mftComment {
					{
						prg.stdout.Writeln()
						prg.stdout.Write("! Only symbolic tokens should appear after %%%")
						prg.error1()
					}
					// \xref[Only symbolic tokens...]

					// \xref[Only symbolic tokens...]
					goto reswitch
				}
			}
			prg.emptyBuffer = true
			goto restart

		case internal, specialTag, tag:
			// Translate a tag and possible subscript
			if int32(prg.byteStart[prg.curTok+1])-int32(prg.byteStart[prg.curTok]) == 1 {
				prg.outName(namePointer(prg.curTok))
			} else {
				prg.outMacAndName(asciiCode('\\'), namePointer(prg.curTok))
			}
			// \xref[\\\\]
			prg.getNext()
			if int32(prg.byteMem[prg.byteStart[prg.prevTok]]) == '\'' {
				goto reswitch
			}
			switch prg.prevType {
			case internal:
				if int32(prg.curType) == numericToken || int32(prg.curType) >= minSuffix {
					if int32(prg.outPtr) == lineLength {
						prg.breakOut()
					}
					prg.outPtr = byte(int32(prg.outPtr) + 1)
					prg.outBuf[prg.outPtr] = '\\'
					if int32(prg.outPtr) == lineLength {
						prg.breakOut()
					}
					prg.outPtr = byte(int32(prg.outPtr) + 1)
					prg.outBuf[prg.outPtr] = ','
				}
				// \xref[\\,]

				// \xref[\\,]
				goto reswitch

			case specialTag:
				if int32(prg.curType) < minSuffix {
					goto reswitch
				} else {
					{
						if int32(prg.outPtr) == lineLength {
							prg.breakOut()
						}
						prg.outPtr = byte(int32(prg.outPtr) + 1)
						prg.outBuf[prg.outPtr] = '.'
					}
					prg.curType = byte(internal)
					goto reswitch
					// \xref[.]
				}
			case tag:
				if int32(prg.curType) == tag {
					if int32(prg.byteMem[prg.byteStart[prg.curTok]]) == '\'' {
						goto reswitch
					}
				} // a sequence of primes goes on the main line
				if int32(prg.curType) == numericToken || int32(prg.curType) >= minSuffix {
					{
						if int32(prg.outPtr) == lineLength {
							prg.breakOut()
						}
						prg.outPtr = byte(int32(prg.outPtr) + 1)
						prg.outBuf[prg.outPtr] = '_'
						if int32(prg.outPtr) == lineLength {
							prg.breakOut()
						}
						prg.outPtr = byte(int32(prg.outPtr) + 1)
						prg.outBuf[prg.outPtr] = '{'
					}
					for true {
						if int32(prg.curType) >= minSuffix {
							prg.outName(namePointer(prg.curTok))
						} else {
							prg.copy(prg.curTok)
						}
						if int32(prg.prevType) == specialTag {
							prg.getNext()
							goto done
						}
						prg.getNext()
						if int32(prg.curType) < minSuffix {
							if int32(prg.curType) != numericToken {
								goto done
							}
						}
						if int32(prg.curType) == int32(prg.prevType) {
							if int32(prg.curType) == numericToken {
								if int32(prg.outPtr) == lineLength {
									prg.breakOut()
								}
								prg.outPtr = byte(int32(prg.outPtr) + 1)
								prg.outBuf[prg.outPtr] = '\\'
								if int32(prg.outPtr) == lineLength {
									prg.breakOut()
								}
								prg.outPtr = byte(int32(prg.outPtr) + 1)
								prg.outBuf[prg.outPtr] = ','
							} else if int32(prg.charClass[prg.byteMem[prg.byteStart[prg.curTok]]]) == int32(prg.charClass[prg.byteMem[prg.byteStart[prg.prevTok]]]) {
								if int32(prg.byteMem[prg.byteStart[prg.prevTok]]) != '.' {
									if int32(prg.outPtr) == lineLength {
										prg.breakOut()
									}
									prg.outPtr = byte(int32(prg.outPtr) + 1)
									prg.outBuf[prg.outPtr] = '.'
								} else {
									if int32(prg.outPtr) == lineLength {
										prg.breakOut()
									}
									prg.outPtr = byte(int32(prg.outPtr) + 1)
									prg.outBuf[prg.outPtr] = '\\'
									if int32(prg.outPtr) == lineLength {
										prg.breakOut()
									}
									prg.outPtr = byte(int32(prg.outPtr) + 1)
									prg.outBuf[prg.outPtr] = ','
								}
							}
						}
					}

				done:
					{
						if int32(prg.outPtr) == lineLength {
							prg.breakOut()
						}
						prg.outPtr = byte(int32(prg.outPtr) + 1)
						prg.outBuf[prg.outPtr] = '}'
					}
					goto reswitch
				} else if int32(prg.curType) == sharp {
					prg.outStr(prg.trSharp)
				} else {
					goto reswitch
				}

			} // there are no other cases

		} // all cases have been listed
	}

exit:
}

// 112. The main program

// tangle:pos mft.ch:102:21:

// Let's put it all together now: \.[MFT] starts and ends here.
// \xref[system dependencies]
func (prg *prg) main() {
	defer func() {
		if prg.changeFile != nil {
			prg.changeFile.Close()
		}
		if prg.mfFile != nil {
			prg.mfFile.Close()
		}
		if prg.stderr != nil {
			prg.stderr.Close()
		}
		if prg.stdin != nil {
			prg.stdin.Close()
		}
		if prg.stdout != nil {
			prg.stdout.Close()
		}
		if prg.styleFile != nil {
			prg.styleFile.Close()
		}
		if prg.texFile != nil {
			prg.texFile.Close()
		}
	}()

	prg.initialize()                                                       // beginning of the main program
	prg.stdout.Writeln("This is MFT, Version 2.1 (gomft v0.0-prerelease)") // print a ``banner line''

	// Store all the primitives
	prg.idLoc = 18

	prg.idFirst = 16
	prg.buffer[16] = '.'
	prg.buffer[17] = '.'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(pathJoin)

	prg.idFirst = 17
	prg.buffer[17] = '['
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(asIs)

	prg.idFirst = 17
	prg.buffer[17] = ']'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(asIs)

	prg.idFirst = 17
	prg.buffer[17] = '}'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(asIs)

	prg.idFirst = 17
	prg.buffer[17] = '{'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(asIs)

	prg.idFirst = 17
	prg.buffer[17] = ':'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(colon)

	prg.idFirst = 16
	prg.buffer[16] = ':'
	prg.buffer[17] = ':'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(colon)

	prg.idFirst = 15
	prg.buffer[15] = '|'
	prg.buffer[16] = '|'
	prg.buffer[17] = ':'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(colon)

	prg.idFirst = 16
	prg.buffer[16] = ':'
	prg.buffer[17] = '='
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(asIs)

	prg.idFirst = 17
	prg.buffer[17] = ','
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(asIs)

	prg.idFirst = 17
	prg.buffer[17] = ';'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(semicolon)

	prg.idFirst = 17
	prg.buffer[17] = '\\'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(backslash)

	prg.idFirst = 16
	prg.buffer[16] = '\\'
	prg.buffer[17] = '\\'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(doubleBack)

	prg.idFirst = 13
	prg.buffer[13] = 'a'
	prg.buffer[14] = 'd'
	prg.buffer[15] = 'd'
	prg.buffer[16] = 't'
	prg.buffer[17] = 'o'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 16
	prg.buffer[16] = 'a'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(bbinary)

	prg.idFirst = 11
	prg.buffer[11] = 'a'
	prg.buffer[12] = 't'
	prg.buffer[13] = 'l'
	prg.buffer[14] = 'e'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 's'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 8
	prg.buffer[8] = 'b'
	prg.buffer[9] = 'e'
	prg.buffer[10] = 'g'
	prg.buffer[11] = 'i'
	prg.buffer[12] = 'n'
	prg.buffer[13] = 'g'
	prg.buffer[14] = 'r'
	prg.buffer[15] = 'o'
	prg.buffer[16] = 'u'
	prg.buffer[17] = 'p'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)
	prg.idFirst = 10
	prg.buffer[10] = 'c'
	prg.buffer[11] = 'o'
	prg.buffer[12] = 'n'
	prg.buffer[13] = 't'
	prg.buffer[14] = 'r'
	prg.buffer[15] = 'o'
	prg.buffer[16] = 'l'
	prg.buffer[17] = 's'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 14
	prg.buffer[14] = 'c'
	prg.buffer[15] = 'u'
	prg.buffer[16] = 'l'
	prg.buffer[17] = 'l'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 14
	prg.buffer[14] = 'c'
	prg.buffer[15] = 'u'
	prg.buffer[16] = 'r'
	prg.buffer[17] = 'l'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 8
	prg.buffer[8] = 'd'
	prg.buffer[9] = 'e'
	prg.buffer[10] = 'l'
	prg.buffer[11] = 'i'
	prg.buffer[12] = 'm'
	prg.buffer[13] = 'i'
	prg.buffer[14] = 't'
	prg.buffer[15] = 'e'
	prg.buffer[16] = 'r'
	prg.buffer[17] = 's'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 11
	prg.buffer[11] = 'd'
	prg.buffer[12] = 'i'
	prg.buffer[13] = 's'
	prg.buffer[14] = 'p'
	prg.buffer[15] = 'l'
	prg.buffer[16] = 'a'
	prg.buffer[17] = 'y'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 10
	prg.buffer[10] = 'e'
	prg.buffer[11] = 'n'
	prg.buffer[12] = 'd'
	prg.buffer[13] = 'g'
	prg.buffer[14] = 'r'
	prg.buffer[15] = 'o'
	prg.buffer[16] = 'u'
	prg.buffer[17] = 'p'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(endit)

	prg.idFirst = 10
	prg.buffer[10] = 'e'
	prg.buffer[11] = 'v'
	prg.buffer[12] = 'e'
	prg.buffer[13] = 'r'
	prg.buffer[14] = 'y'
	prg.buffer[15] = 'j'
	prg.buffer[16] = 'o'
	prg.buffer[17] = 'b'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 12
	prg.buffer[12] = 'e'
	prg.buffer[13] = 'x'
	prg.buffer[14] = 'i'
	prg.buffer[15] = 't'
	prg.buffer[16] = 'i'
	prg.buffer[17] = 'f'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 7
	prg.buffer[7] = 'e'
	prg.buffer[8] = 'x'
	prg.buffer[9] = 'p'
	prg.buffer[10] = 'a'
	prg.buffer[11] = 'n'
	prg.buffer[12] = 'd'
	prg.buffer[13] = 'a'
	prg.buffer[14] = 'f'
	prg.buffer[15] = 't'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'r'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 14
	prg.buffer[14] = 'f'
	prg.buffer[15] = 'r'
	prg.buffer[16] = 'o'
	prg.buffer[17] = 'm'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(bbinary)

	prg.idFirst = 10
	prg.buffer[10] = 'i'
	prg.buffer[11] = 'n'
	prg.buffer[12] = 'w'
	prg.buffer[13] = 'i'
	prg.buffer[14] = 'n'
	prg.buffer[15] = 'd'
	prg.buffer[16] = 'o'
	prg.buffer[17] = 'w'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(bbinary)

	prg.idFirst = 11
	prg.buffer[11] = 'i'
	prg.buffer[12] = 'n'
	prg.buffer[13] = 't'
	prg.buffer[14] = 'e'
	prg.buffer[15] = 'r'
	prg.buffer[16] = 'i'
	prg.buffer[17] = 'm'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 15
	prg.buffer[15] = 'l'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 7
	prg.buffer[7] = 'n'
	prg.buffer[8] = 'e'
	prg.buffer[9] = 'w'
	prg.buffer[10] = 'i'
	prg.buffer[11] = 'n'
	prg.buffer[12] = 't'
	prg.buffer[13] = 'e'
	prg.buffer[14] = 'r'
	prg.buffer[15] = 'n'
	prg.buffer[16] = 'a'
	prg.buffer[17] = 'l'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 16
	prg.buffer[16] = 'o'
	prg.buffer[17] = 'f'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 8
	prg.buffer[8] = 'o'
	prg.buffer[9] = 'p'
	prg.buffer[10] = 'e'
	prg.buffer[11] = 'n'
	prg.buffer[12] = 'w'
	prg.buffer[13] = 'i'
	prg.buffer[14] = 'n'
	prg.buffer[15] = 'd'
	prg.buffer[16] = 'o'
	prg.buffer[17] = 'w'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 8
	prg.buffer[8] = 'r'
	prg.buffer[9] = 'a'
	prg.buffer[10] = 'n'
	prg.buffer[11] = 'd'
	prg.buffer[12] = 'o'
	prg.buffer[13] = 'm'
	prg.buffer[14] = 's'
	prg.buffer[15] = 'e'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'd'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 14
	prg.buffer[14] = 's'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 'v'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 8
	prg.buffer[8] = 's'
	prg.buffer[9] = 'c'
	prg.buffer[10] = 'a'
	prg.buffer[11] = 'n'
	prg.buffer[12] = 't'
	prg.buffer[13] = 'o'
	prg.buffer[14] = 'k'
	prg.buffer[15] = 'e'
	prg.buffer[16] = 'n'
	prg.buffer[17] = 's'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 11
	prg.buffer[11] = 's'
	prg.buffer[12] = 'h'
	prg.buffer[13] = 'i'
	prg.buffer[14] = 'p'
	prg.buffer[15] = 'o'
	prg.buffer[16] = 'u'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 14
	prg.buffer[14] = 's'
	prg.buffer[15] = 't'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'p'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(bbinary)

	prg.idFirst = 15
	prg.buffer[15] = 's'
	prg.buffer[16] = 't'
	prg.buffer[17] = 'r'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 11
	prg.buffer[11] = 't'
	prg.buffer[12] = 'e'
	prg.buffer[13] = 'n'
	prg.buffer[14] = 's'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 'o'
	prg.buffer[17] = 'n'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 16
	prg.buffer[16] = 't'
	prg.buffer[17] = 'o'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(bbinary)

	prg.idFirst = 13
	prg.buffer[13] = 'u'
	prg.buffer[14] = 'n'
	prg.buffer[15] = 't'
	prg.buffer[16] = 'i'
	prg.buffer[17] = 'l'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(bbinary)

	prg.idFirst = 15
	prg.buffer[15] = 'd'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'f'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 12
	prg.buffer[12] = 'v'
	prg.buffer[13] = 'a'
	prg.buffer[14] = 'r'
	prg.buffer[15] = 'd'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'f'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 8
	prg.buffer[8] = 'p'
	prg.buffer[9] = 'r'
	prg.buffer[10] = 'i'
	prg.buffer[11] = 'm'
	prg.buffer[12] = 'a'
	prg.buffer[13] = 'r'
	prg.buffer[14] = 'y'
	prg.buffer[15] = 'd'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'f'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 6
	prg.buffer[6] = 's'
	prg.buffer[7] = 'e'
	prg.buffer[8] = 'c'
	prg.buffer[9] = 'o'
	prg.buffer[10] = 'n'
	prg.buffer[11] = 'd'
	prg.buffer[12] = 'a'
	prg.buffer[13] = 'r'
	prg.buffer[14] = 'y'
	prg.buffer[15] = 'd'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'f'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 7
	prg.buffer[7] = 't'
	prg.buffer[8] = 'e'
	prg.buffer[9] = 'r'
	prg.buffer[10] = 't'
	prg.buffer[11] = 'i'
	prg.buffer[12] = 'a'
	prg.buffer[13] = 'r'
	prg.buffer[14] = 'y'
	prg.buffer[15] = 'd'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'f'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 12
	prg.buffer[12] = 'e'
	prg.buffer[13] = 'n'
	prg.buffer[14] = 'd'
	prg.buffer[15] = 'd'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'f'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(endit)

	prg.idFirst = 15
	prg.buffer[15] = 'f'
	prg.buffer[16] = 'o'
	prg.buffer[17] = 'r'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 7
	prg.buffer[7] = 'f'
	prg.buffer[8] = 'o'
	prg.buffer[9] = 'r'
	prg.buffer[10] = 's'
	prg.buffer[11] = 'u'
	prg.buffer[12] = 'f'
	prg.buffer[13] = 'f'
	prg.buffer[14] = 'i'
	prg.buffer[15] = 'x'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 's'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 11
	prg.buffer[11] = 'f'
	prg.buffer[12] = 'o'
	prg.buffer[13] = 'r'
	prg.buffer[14] = 'e'
	prg.buffer[15] = 'v'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'r'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 12
	prg.buffer[12] = 'e'
	prg.buffer[13] = 'n'
	prg.buffer[14] = 'd'
	prg.buffer[15] = 'f'
	prg.buffer[16] = 'o'
	prg.buffer[17] = 'r'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(endit)

	prg.idFirst = 13
	prg.buffer[13] = 'q'
	prg.buffer[14] = 'u'
	prg.buffer[15] = 'o'
	prg.buffer[16] = 't'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 14
	prg.buffer[14] = 'e'
	prg.buffer[15] = 'x'
	prg.buffer[16] = 'p'
	prg.buffer[17] = 'r'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 12
	prg.buffer[12] = 's'
	prg.buffer[13] = 'u'
	prg.buffer[14] = 'f'
	prg.buffer[15] = 'f'
	prg.buffer[16] = 'i'
	prg.buffer[17] = 'x'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 14
	prg.buffer[14] = 't'
	prg.buffer[15] = 'e'
	prg.buffer[16] = 'x'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 11
	prg.buffer[11] = 'p'
	prg.buffer[12] = 'r'
	prg.buffer[13] = 'i'
	prg.buffer[14] = 'm'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 'r'
	prg.buffer[17] = 'y'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 9
	prg.buffer[9] = 's'
	prg.buffer[10] = 'e'
	prg.buffer[11] = 'c'
	prg.buffer[12] = 'o'
	prg.buffer[13] = 'n'
	prg.buffer[14] = 'd'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 'r'
	prg.buffer[17] = 'y'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 10
	prg.buffer[10] = 't'
	prg.buffer[11] = 'e'
	prg.buffer[12] = 'r'
	prg.buffer[13] = 't'
	prg.buffer[14] = 'i'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 'r'
	prg.buffer[17] = 'y'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 13
	prg.buffer[13] = 'i'
	prg.buffer[14] = 'n'
	prg.buffer[15] = 'p'
	prg.buffer[16] = 'u'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(inputCommand)

	prg.idFirst = 10
	prg.buffer[10] = 'e'
	prg.buffer[11] = 'n'
	prg.buffer[12] = 'd'
	prg.buffer[13] = 'i'
	prg.buffer[14] = 'n'
	prg.buffer[15] = 'p'
	prg.buffer[16] = 'u'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(bold)

	prg.idFirst = 16
	prg.buffer[16] = 'i'
	prg.buffer[17] = 'f'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 16
	prg.buffer[16] = 'f'
	prg.buffer[17] = 'i'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(endit)

	prg.idFirst = 14
	prg.buffer[14] = 'e'
	prg.buffer[15] = 'l'
	prg.buffer[16] = 's'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 12
	prg.buffer[12] = 'e'
	prg.buffer[13] = 'l'
	prg.buffer[14] = 's'
	prg.buffer[15] = 'e'
	prg.buffer[16] = 'i'
	prg.buffer[17] = 'f'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 14
	prg.buffer[14] = 't'
	prg.buffer[15] = 'r'
	prg.buffer[16] = 'u'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(bold)

	prg.idFirst = 13
	prg.buffer[13] = 'f'
	prg.buffer[14] = 'a'
	prg.buffer[15] = 'l'
	prg.buffer[16] = 's'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(bold)

	prg.idFirst = 7
	prg.buffer[7] = 'n'
	prg.buffer[8] = 'u'
	prg.buffer[9] = 'l'
	prg.buffer[10] = 'l'
	prg.buffer[11] = 'p'
	prg.buffer[12] = 'i'
	prg.buffer[13] = 'c'
	prg.buffer[14] = 't'
	prg.buffer[15] = 'u'
	prg.buffer[16] = 'r'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(bold)

	prg.idFirst = 11
	prg.buffer[11] = 'n'
	prg.buffer[12] = 'u'
	prg.buffer[13] = 'l'
	prg.buffer[14] = 'l'
	prg.buffer[15] = 'p'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'n'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(bold)

	prg.idFirst = 11
	prg.buffer[11] = 'j'
	prg.buffer[12] = 'o'
	prg.buffer[13] = 'b'
	prg.buffer[14] = 'n'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 'm'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(bold)

	prg.idFirst = 8
	prg.buffer[8] = 'r'
	prg.buffer[9] = 'e'
	prg.buffer[10] = 'a'
	prg.buffer[11] = 'd'
	prg.buffer[12] = 's'
	prg.buffer[13] = 't'
	prg.buffer[14] = 'r'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 'n'
	prg.buffer[17] = 'g'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(bold)

	prg.idFirst = 9
	prg.buffer[9] = 'p'
	prg.buffer[10] = 'e'
	prg.buffer[11] = 'n'
	prg.buffer[12] = 'c'
	prg.buffer[13] = 'i'
	prg.buffer[14] = 'r'
	prg.buffer[15] = 'c'
	prg.buffer[16] = 'l'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(bold)

	prg.idFirst = 14
	prg.buffer[14] = 'g'
	prg.buffer[15] = 'o'
	prg.buffer[16] = 'o'
	prg.buffer[17] = 'd'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(specialTag)

	prg.idFirst = 16
	prg.buffer[16] = '='
	prg.buffer[17] = ':'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(asIs)

	prg.idFirst = 15
	prg.buffer[15] = '='
	prg.buffer[16] = ':'
	prg.buffer[17] = '|'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(asIs)

	prg.idFirst = 14
	prg.buffer[14] = '='
	prg.buffer[15] = ':'
	prg.buffer[16] = '|'
	prg.buffer[17] = '>'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(asIs)

	prg.idFirst = 15
	prg.buffer[15] = '|'
	prg.buffer[16] = '='
	prg.buffer[17] = ':'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(asIs)

	prg.idFirst = 14
	prg.buffer[14] = '|'
	prg.buffer[15] = '='
	prg.buffer[16] = ':'
	prg.buffer[17] = '>'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(asIs)

	prg.idFirst = 14
	prg.buffer[14] = '|'
	prg.buffer[15] = '='
	prg.buffer[16] = ':'
	prg.buffer[17] = '|'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(asIs)

	prg.idFirst = 13
	prg.buffer[13] = '|'
	prg.buffer[14] = '='
	prg.buffer[15] = ':'
	prg.buffer[16] = '|'
	prg.buffer[17] = '>'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(asIs)

	prg.idFirst = 12
	prg.buffer[12] = '|'
	prg.buffer[13] = '='
	prg.buffer[14] = ':'
	prg.buffer[15] = '|'
	prg.buffer[16] = '>'
	prg.buffer[17] = '>'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(asIs)

	prg.idFirst = 14
	prg.buffer[14] = 'k'
	prg.buffer[15] = 'e'
	prg.buffer[16] = 'r'
	prg.buffer[17] = 'n'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(binary)
	prg.idFirst = 12
	prg.buffer[12] = 's'
	prg.buffer[13] = 'k'
	prg.buffer[14] = 'i'
	prg.buffer[15] = 'p'
	prg.buffer[16] = 't'
	prg.buffer[17] = 'o'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 5
	prg.buffer[5] = 'n'
	prg.buffer[6] = 'o'
	prg.buffer[7] = 'r'
	prg.buffer[8] = 'm'
	prg.buffer[9] = 'a'
	prg.buffer[10] = 'l'
	prg.buffer[11] = 'd'
	prg.buffer[12] = 'e'
	prg.buffer[13] = 'v'
	prg.buffer[14] = 'i'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 't'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 15
	prg.buffer[15] = 'o'
	prg.buffer[16] = 'd'
	prg.buffer[17] = 'd'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 13
	prg.buffer[13] = 'k'
	prg.buffer[14] = 'n'
	prg.buffer[15] = 'o'
	prg.buffer[16] = 'w'
	prg.buffer[17] = 'n'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 11
	prg.buffer[11] = 'u'
	prg.buffer[12] = 'n'
	prg.buffer[13] = 'k'
	prg.buffer[14] = 'n'
	prg.buffer[15] = 'o'
	prg.buffer[16] = 'w'
	prg.buffer[17] = 'n'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 15
	prg.buffer[15] = 'n'
	prg.buffer[16] = 'o'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 11
	prg.buffer[11] = 'd'
	prg.buffer[12] = 'e'
	prg.buffer[13] = 'c'
	prg.buffer[14] = 'i'
	prg.buffer[15] = 'm'
	prg.buffer[16] = 'a'
	prg.buffer[17] = 'l'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 11
	prg.buffer[11] = 'r'
	prg.buffer[12] = 'e'
	prg.buffer[13] = 'v'
	prg.buffer[14] = 'e'
	prg.buffer[15] = 'r'
	prg.buffer[16] = 's'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 10
	prg.buffer[10] = 'm'
	prg.buffer[11] = 'a'
	prg.buffer[12] = 'k'
	prg.buffer[13] = 'e'
	prg.buffer[14] = 'p'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 't'
	prg.buffer[17] = 'h'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 11
	prg.buffer[11] = 'm'
	prg.buffer[12] = 'a'
	prg.buffer[13] = 'k'
	prg.buffer[14] = 'e'
	prg.buffer[15] = 'p'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'n'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 7
	prg.buffer[7] = 't'
	prg.buffer[8] = 'o'
	prg.buffer[9] = 't'
	prg.buffer[10] = 'a'
	prg.buffer[11] = 'l'
	prg.buffer[12] = 'w'
	prg.buffer[13] = 'e'
	prg.buffer[14] = 'i'
	prg.buffer[15] = 'g'
	prg.buffer[16] = 'h'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 15
	prg.buffer[15] = 'o'
	prg.buffer[16] = 'c'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 15
	prg.buffer[15] = 'h'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'x'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 13
	prg.buffer[13] = 'A'
	prg.buffer[14] = 'S'
	prg.buffer[15] = 'C'
	prg.buffer[16] = 'I'
	prg.buffer[17] = 'I'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 14
	prg.buffer[14] = 'c'
	prg.buffer[15] = 'h'
	prg.buffer[16] = 'a'
	prg.buffer[17] = 'r'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 12
	prg.buffer[12] = 'l'
	prg.buffer[13] = 'e'
	prg.buffer[14] = 'n'
	prg.buffer[15] = 'g'
	prg.buffer[16] = 't'
	prg.buffer[17] = 'h'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 5
	prg.buffer[5] = 't'
	prg.buffer[6] = 'u'
	prg.buffer[7] = 'r'
	prg.buffer[8] = 'n'
	prg.buffer[9] = 'i'
	prg.buffer[10] = 'n'
	prg.buffer[11] = 'g'
	prg.buffer[12] = 'n'
	prg.buffer[13] = 'u'
	prg.buffer[14] = 'm'
	prg.buffer[15] = 'b'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'r'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 13
	prg.buffer[13] = 'x'
	prg.buffer[14] = 'p'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 'r'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 13
	prg.buffer[13] = 'y'
	prg.buffer[14] = 'p'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 'r'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 12
	prg.buffer[12] = 'x'
	prg.buffer[13] = 'x'
	prg.buffer[14] = 'p'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 'r'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 12
	prg.buffer[12] = 'x'
	prg.buffer[13] = 'y'
	prg.buffer[14] = 'p'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 'r'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 12
	prg.buffer[12] = 'y'
	prg.buffer[13] = 'x'
	prg.buffer[14] = 'p'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 'r'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 12
	prg.buffer[12] = 'y'
	prg.buffer[13] = 'y'
	prg.buffer[14] = 'p'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 'r'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 14
	prg.buffer[14] = 's'
	prg.buffer[15] = 'q'
	prg.buffer[16] = 'r'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 14
	prg.buffer[14] = 'm'
	prg.buffer[15] = 'e'
	prg.buffer[16] = 'x'
	prg.buffer[17] = 'p'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 14
	prg.buffer[14] = 'm'
	prg.buffer[15] = 'l'
	prg.buffer[16] = 'o'
	prg.buffer[17] = 'g'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 14
	prg.buffer[14] = 's'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 'n'
	prg.buffer[17] = 'd'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 14
	prg.buffer[14] = 'c'
	prg.buffer[15] = 'o'
	prg.buffer[16] = 's'
	prg.buffer[17] = 'd'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 13
	prg.buffer[13] = 'f'
	prg.buffer[14] = 'l'
	prg.buffer[15] = 'o'
	prg.buffer[16] = 'o'
	prg.buffer[17] = 'r'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 4
	prg.buffer[4] = 'u'
	prg.buffer[5] = 'n'
	prg.buffer[6] = 'i'
	prg.buffer[7] = 'f'
	prg.buffer[8] = 'o'
	prg.buffer[9] = 'r'
	prg.buffer[10] = 'm'
	prg.buffer[11] = 'd'
	prg.buffer[12] = 'e'
	prg.buffer[13] = 'v'
	prg.buffer[14] = 'i'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 't'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 8
	prg.buffer[8] = 'c'
	prg.buffer[9] = 'h'
	prg.buffer[10] = 'a'
	prg.buffer[11] = 'r'
	prg.buffer[12] = 'e'
	prg.buffer[13] = 'x'
	prg.buffer[14] = 'i'
	prg.buffer[15] = 's'
	prg.buffer[16] = 't'
	prg.buffer[17] = 's'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 13
	prg.buffer[13] = 'a'
	prg.buffer[14] = 'n'
	prg.buffer[15] = 'g'
	prg.buffer[16] = 'l'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 13
	prg.buffer[13] = 'c'
	prg.buffer[14] = 'y'
	prg.buffer[15] = 'c'
	prg.buffer[16] = 'l'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(op)

	prg.idFirst = 5
	prg.buffer[5] = 't'
	prg.buffer[6] = 'r'
	prg.buffer[7] = 'a'
	prg.buffer[8] = 'c'
	prg.buffer[9] = 'i'
	prg.buffer[10] = 'n'
	prg.buffer[11] = 'g'
	prg.buffer[12] = 't'
	prg.buffer[13] = 'i'
	prg.buffer[14] = 't'
	prg.buffer[15] = 'l'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 's'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 2
	prg.buffer[2] = 't'
	prg.buffer[3] = 'r'
	prg.buffer[4] = 'a'
	prg.buffer[5] = 'c'
	prg.buffer[6] = 'i'
	prg.buffer[7] = 'n'
	prg.buffer[8] = 'g'
	prg.buffer[9] = 'e'
	prg.buffer[10] = 'q'
	prg.buffer[11] = 'u'
	prg.buffer[12] = 'a'
	prg.buffer[13] = 't'
	prg.buffer[14] = 'i'
	prg.buffer[15] = 'o'
	prg.buffer[16] = 'n'
	prg.buffer[17] = 's'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 3
	prg.buffer[3] = 't'
	prg.buffer[4] = 'r'
	prg.buffer[5] = 'a'
	prg.buffer[6] = 'c'
	prg.buffer[7] = 'i'
	prg.buffer[8] = 'n'
	prg.buffer[9] = 'g'
	prg.buffer[10] = 'c'
	prg.buffer[11] = 'a'
	prg.buffer[12] = 'p'
	prg.buffer[13] = 's'
	prg.buffer[14] = 'u'
	prg.buffer[15] = 'l'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 's'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 4
	prg.buffer[4] = 't'
	prg.buffer[5] = 'r'
	prg.buffer[6] = 'a'
	prg.buffer[7] = 'c'
	prg.buffer[8] = 'i'
	prg.buffer[9] = 'n'
	prg.buffer[10] = 'g'
	prg.buffer[11] = 'c'
	prg.buffer[12] = 'h'
	prg.buffer[13] = 'o'
	prg.buffer[14] = 'i'
	prg.buffer[15] = 'c'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 's'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 6
	prg.buffer[6] = 't'
	prg.buffer[7] = 'r'
	prg.buffer[8] = 'a'
	prg.buffer[9] = 'c'
	prg.buffer[10] = 'i'
	prg.buffer[11] = 'n'
	prg.buffer[12] = 'g'
	prg.buffer[13] = 's'
	prg.buffer[14] = 'p'
	prg.buffer[15] = 'e'
	prg.buffer[16] = 'c'
	prg.buffer[17] = 's'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 7
	prg.buffer[7] = 't'
	prg.buffer[8] = 'r'
	prg.buffer[9] = 'a'
	prg.buffer[10] = 'c'
	prg.buffer[11] = 'i'
	prg.buffer[12] = 'n'
	prg.buffer[13] = 'g'
	prg.buffer[14] = 'p'
	prg.buffer[15] = 'e'
	prg.buffer[16] = 'n'
	prg.buffer[17] = 's'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 3
	prg.buffer[3] = 't'
	prg.buffer[4] = 'r'
	prg.buffer[5] = 'a'
	prg.buffer[6] = 'c'
	prg.buffer[7] = 'i'
	prg.buffer[8] = 'n'
	prg.buffer[9] = 'g'
	prg.buffer[10] = 'c'
	prg.buffer[11] = 'o'
	prg.buffer[12] = 'm'
	prg.buffer[13] = 'm'
	prg.buffer[14] = 'a'
	prg.buffer[15] = 'n'
	prg.buffer[16] = 'd'
	prg.buffer[17] = 's'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 5
	prg.buffer[5] = 't'
	prg.buffer[6] = 'r'
	prg.buffer[7] = 'a'
	prg.buffer[8] = 'c'
	prg.buffer[9] = 'i'
	prg.buffer[10] = 'n'
	prg.buffer[11] = 'g'
	prg.buffer[12] = 'm'
	prg.buffer[13] = 'a'
	prg.buffer[14] = 'c'
	prg.buffer[15] = 'r'
	prg.buffer[16] = 'o'
	prg.buffer[17] = 's'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 6
	prg.buffer[6] = 't'
	prg.buffer[7] = 'r'
	prg.buffer[8] = 'a'
	prg.buffer[9] = 'c'
	prg.buffer[10] = 'i'
	prg.buffer[11] = 'n'
	prg.buffer[12] = 'g'
	prg.buffer[13] = 'e'
	prg.buffer[14] = 'd'
	prg.buffer[15] = 'g'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 's'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 5
	prg.buffer[5] = 't'
	prg.buffer[6] = 'r'
	prg.buffer[7] = 'a'
	prg.buffer[8] = 'c'
	prg.buffer[9] = 'i'
	prg.buffer[10] = 'n'
	prg.buffer[11] = 'g'
	prg.buffer[12] = 'o'
	prg.buffer[13] = 'u'
	prg.buffer[14] = 't'
	prg.buffer[15] = 'p'
	prg.buffer[16] = 'u'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 6
	prg.buffer[6] = 't'
	prg.buffer[7] = 'r'
	prg.buffer[8] = 'a'
	prg.buffer[9] = 'c'
	prg.buffer[10] = 'i'
	prg.buffer[11] = 'n'
	prg.buffer[12] = 'g'
	prg.buffer[13] = 's'
	prg.buffer[14] = 't'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 't'
	prg.buffer[17] = 's'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 5
	prg.buffer[5] = 't'
	prg.buffer[6] = 'r'
	prg.buffer[7] = 'a'
	prg.buffer[8] = 'c'
	prg.buffer[9] = 'i'
	prg.buffer[10] = 'n'
	prg.buffer[11] = 'g'
	prg.buffer[12] = 'o'
	prg.buffer[13] = 'n'
	prg.buffer[14] = 'l'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 'n'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 3
	prg.buffer[3] = 't'
	prg.buffer[4] = 'r'
	prg.buffer[5] = 'a'
	prg.buffer[6] = 'c'
	prg.buffer[7] = 'i'
	prg.buffer[8] = 'n'
	prg.buffer[9] = 'g'
	prg.buffer[10] = 'r'
	prg.buffer[11] = 'e'
	prg.buffer[12] = 's'
	prg.buffer[13] = 't'
	prg.buffer[14] = 'o'
	prg.buffer[15] = 'r'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 's'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 14
	prg.buffer[14] = 'y'
	prg.buffer[15] = 'e'
	prg.buffer[16] = 'a'
	prg.buffer[17] = 'r'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 13
	prg.buffer[13] = 'm'
	prg.buffer[14] = 'o'
	prg.buffer[15] = 'n'
	prg.buffer[16] = 't'
	prg.buffer[17] = 'h'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 15
	prg.buffer[15] = 'd'
	prg.buffer[16] = 'a'
	prg.buffer[17] = 'y'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 14
	prg.buffer[14] = 't'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 'm'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 10
	prg.buffer[10] = 'c'
	prg.buffer[11] = 'h'
	prg.buffer[12] = 'a'
	prg.buffer[13] = 'r'
	prg.buffer[14] = 'c'
	prg.buffer[15] = 'o'
	prg.buffer[16] = 'd'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 11
	prg.buffer[11] = 'c'
	prg.buffer[12] = 'h'
	prg.buffer[13] = 'a'
	prg.buffer[14] = 'r'
	prg.buffer[15] = 'e'
	prg.buffer[16] = 'x'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 12
	prg.buffer[12] = 'c'
	prg.buffer[13] = 'h'
	prg.buffer[14] = 'a'
	prg.buffer[15] = 'r'
	prg.buffer[16] = 'w'
	prg.buffer[17] = 'd'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 12
	prg.buffer[12] = 'c'
	prg.buffer[13] = 'h'
	prg.buffer[14] = 'a'
	prg.buffer[15] = 'r'
	prg.buffer[16] = 'h'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 12
	prg.buffer[12] = 'c'
	prg.buffer[13] = 'h'
	prg.buffer[14] = 'a'
	prg.buffer[15] = 'r'
	prg.buffer[16] = 'd'
	prg.buffer[17] = 'p'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 12
	prg.buffer[12] = 'c'
	prg.buffer[13] = 'h'
	prg.buffer[14] = 'a'
	prg.buffer[15] = 'r'
	prg.buffer[16] = 'i'
	prg.buffer[17] = 'c'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 12
	prg.buffer[12] = 'c'
	prg.buffer[13] = 'h'
	prg.buffer[14] = 'a'
	prg.buffer[15] = 'r'
	prg.buffer[16] = 'd'
	prg.buffer[17] = 'x'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 12
	prg.buffer[12] = 'c'
	prg.buffer[13] = 'h'
	prg.buffer[14] = 'a'
	prg.buffer[15] = 'r'
	prg.buffer[16] = 'd'
	prg.buffer[17] = 'y'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 8
	prg.buffer[8] = 'd'
	prg.buffer[9] = 'e'
	prg.buffer[10] = 's'
	prg.buffer[11] = 'i'
	prg.buffer[12] = 'g'
	prg.buffer[13] = 'n'
	prg.buffer[14] = 's'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 'z'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 14
	prg.buffer[14] = 'h'
	prg.buffer[15] = 'p'
	prg.buffer[16] = 'p'
	prg.buffer[17] = 'p'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 14
	prg.buffer[14] = 'v'
	prg.buffer[15] = 'p'
	prg.buffer[16] = 'p'
	prg.buffer[17] = 'p'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 11
	prg.buffer[11] = 'x'
	prg.buffer[12] = 'o'
	prg.buffer[13] = 'f'
	prg.buffer[14] = 'f'
	prg.buffer[15] = 's'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 11
	prg.buffer[11] = 'y'
	prg.buffer[12] = 'o'
	prg.buffer[13] = 'f'
	prg.buffer[14] = 'f'
	prg.buffer[15] = 's'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 11
	prg.buffer[11] = 'p'
	prg.buffer[12] = 'a'
	prg.buffer[13] = 'u'
	prg.buffer[14] = 's'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 'n'
	prg.buffer[17] = 'g'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 6
	prg.buffer[6] = 's'
	prg.buffer[7] = 'h'
	prg.buffer[8] = 'o'
	prg.buffer[9] = 'w'
	prg.buffer[10] = 's'
	prg.buffer[11] = 't'
	prg.buffer[12] = 'o'
	prg.buffer[13] = 'p'
	prg.buffer[14] = 'p'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 'n'
	prg.buffer[17] = 'g'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 8
	prg.buffer[8] = 'f'
	prg.buffer[9] = 'o'
	prg.buffer[10] = 'n'
	prg.buffer[11] = 't'
	prg.buffer[12] = 'm'
	prg.buffer[13] = 'a'
	prg.buffer[14] = 'k'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 'n'
	prg.buffer[17] = 'g'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 10
	prg.buffer[10] = 'p'
	prg.buffer[11] = 'r'
	prg.buffer[12] = 'o'
	prg.buffer[13] = 'o'
	prg.buffer[14] = 'f'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 'n'
	prg.buffer[17] = 'g'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 9
	prg.buffer[9] = 's'
	prg.buffer[10] = 'm'
	prg.buffer[11] = 'o'
	prg.buffer[12] = 'o'
	prg.buffer[13] = 't'
	prg.buffer[14] = 'h'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 'n'
	prg.buffer[17] = 'g'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 6
	prg.buffer[6] = 'a'
	prg.buffer[7] = 'u'
	prg.buffer[8] = 't'
	prg.buffer[9] = 'o'
	prg.buffer[10] = 'r'
	prg.buffer[11] = 'o'
	prg.buffer[12] = 'u'
	prg.buffer[13] = 'n'
	prg.buffer[14] = 'd'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 'n'
	prg.buffer[17] = 'g'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 7
	prg.buffer[7] = 'g'
	prg.buffer[8] = 'r'
	prg.buffer[9] = 'a'
	prg.buffer[10] = 'n'
	prg.buffer[11] = 'u'
	prg.buffer[12] = 'l'
	prg.buffer[13] = 'a'
	prg.buffer[14] = 'r'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 't'
	prg.buffer[17] = 'y'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 12
	prg.buffer[12] = 'f'
	prg.buffer[13] = 'i'
	prg.buffer[14] = 'l'
	prg.buffer[15] = 'l'
	prg.buffer[16] = 'i'
	prg.buffer[17] = 'n'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 6
	prg.buffer[6] = 't'
	prg.buffer[7] = 'u'
	prg.buffer[8] = 'r'
	prg.buffer[9] = 'n'
	prg.buffer[10] = 'i'
	prg.buffer[11] = 'n'
	prg.buffer[12] = 'g'
	prg.buffer[13] = 'c'
	prg.buffer[14] = 'h'
	prg.buffer[15] = 'e'
	prg.buffer[16] = 'c'
	prg.buffer[17] = 'k'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 6
	prg.buffer[6] = 'w'
	prg.buffer[7] = 'a'
	prg.buffer[8] = 'r'
	prg.buffer[9] = 'n'
	prg.buffer[10] = 'i'
	prg.buffer[11] = 'n'
	prg.buffer[12] = 'g'
	prg.buffer[13] = 'c'
	prg.buffer[14] = 'h'
	prg.buffer[15] = 'e'
	prg.buffer[16] = 'c'
	prg.buffer[17] = 'k'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 6
	prg.buffer[6] = 'b'
	prg.buffer[7] = 'o'
	prg.buffer[8] = 'u'
	prg.buffer[9] = 'n'
	prg.buffer[10] = 'd'
	prg.buffer[11] = 'a'
	prg.buffer[12] = 'r'
	prg.buffer[13] = 'y'
	prg.buffer[14] = 'c'
	prg.buffer[15] = 'h'
	prg.buffer[16] = 'a'
	prg.buffer[17] = 'r'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(internal)

	prg.idFirst = 17
	prg.buffer[17] = '+'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(abinary)

	prg.idFirst = 17
	prg.buffer[17] = '-'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(abinary)

	prg.idFirst = 17
	prg.buffer[17] = '*'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(abinary)

	prg.idFirst = 17
	prg.buffer[17] = '/'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(asIs)

	prg.idFirst = 16
	prg.buffer[16] = '+'
	prg.buffer[17] = '+'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(binary)

	prg.idFirst = 15
	prg.buffer[15] = '+'
	prg.buffer[16] = '-'
	prg.buffer[17] = '+'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(pythSub)

	prg.idFirst = 15
	prg.buffer[15] = 'a'
	prg.buffer[16] = 'n'
	prg.buffer[17] = 'd'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(binary)

	prg.idFirst = 16
	prg.buffer[16] = 'o'
	prg.buffer[17] = 'r'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(binary)

	prg.idFirst = 17
	prg.buffer[17] = '<'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(asIs)

	prg.idFirst = 16
	prg.buffer[16] = '<'
	prg.buffer[17] = '='
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(lessOrEqual)

	prg.idFirst = 17
	prg.buffer[17] = '>'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(asIs)

	prg.idFirst = 16
	prg.buffer[16] = '>'
	prg.buffer[17] = '='
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(greaterOrEqual)

	prg.idFirst = 17
	prg.buffer[17] = '='
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(asIs)

	prg.idFirst = 16
	prg.buffer[16] = '<'
	prg.buffer[17] = '>'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(notEqual)

	prg.idFirst = 9
	prg.buffer[9] = 's'
	prg.buffer[10] = 'u'
	prg.buffer[11] = 'b'
	prg.buffer[12] = 's'
	prg.buffer[13] = 't'
	prg.buffer[14] = 'r'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 'n'
	prg.buffer[17] = 'g'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 11
	prg.buffer[11] = 's'
	prg.buffer[12] = 'u'
	prg.buffer[13] = 'b'
	prg.buffer[14] = 'p'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 't'
	prg.buffer[17] = 'h'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 5
	prg.buffer[5] = 'd'
	prg.buffer[6] = 'i'
	prg.buffer[7] = 'r'
	prg.buffer[8] = 'e'
	prg.buffer[9] = 'c'
	prg.buffer[10] = 't'
	prg.buffer[11] = 'i'
	prg.buffer[12] = 'o'
	prg.buffer[13] = 'n'
	prg.buffer[14] = 't'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 'm'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 13
	prg.buffer[13] = 'p'
	prg.buffer[14] = 'o'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 'n'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 8
	prg.buffer[8] = 'p'
	prg.buffer[9] = 'r'
	prg.buffer[10] = 'e'
	prg.buffer[11] = 'c'
	prg.buffer[12] = 'o'
	prg.buffer[13] = 'n'
	prg.buffer[14] = 't'
	prg.buffer[15] = 'r'
	prg.buffer[16] = 'o'
	prg.buffer[17] = 'l'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 7
	prg.buffer[7] = 'p'
	prg.buffer[8] = 'o'
	prg.buffer[9] = 's'
	prg.buffer[10] = 't'
	prg.buffer[11] = 'c'
	prg.buffer[12] = 'o'
	prg.buffer[13] = 'n'
	prg.buffer[14] = 't'
	prg.buffer[15] = 'r'
	prg.buffer[16] = 'o'
	prg.buffer[17] = 'l'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 9
	prg.buffer[9] = 'p'
	prg.buffer[10] = 'e'
	prg.buffer[11] = 'n'
	prg.buffer[12] = 'o'
	prg.buffer[13] = 'f'
	prg.buffer[14] = 'f'
	prg.buffer[15] = 's'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 17
	prg.buffer[17] = '&'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(ampersand)

	prg.idFirst = 11
	prg.buffer[11] = 'r'
	prg.buffer[12] = 'o'
	prg.buffer[13] = 't'
	prg.buffer[14] = 'a'
	prg.buffer[15] = 't'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'd'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(binary)

	prg.idFirst = 11
	prg.buffer[11] = 's'
	prg.buffer[12] = 'l'
	prg.buffer[13] = 'a'
	prg.buffer[14] = 'n'
	prg.buffer[15] = 't'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'd'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(binary)

	prg.idFirst = 12
	prg.buffer[12] = 's'
	prg.buffer[13] = 'c'
	prg.buffer[14] = 'a'
	prg.buffer[15] = 'l'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'd'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(binary)

	prg.idFirst = 11
	prg.buffer[11] = 's'
	prg.buffer[12] = 'h'
	prg.buffer[13] = 'i'
	prg.buffer[14] = 'f'
	prg.buffer[15] = 't'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'd'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(binary)

	prg.idFirst = 7
	prg.buffer[7] = 't'
	prg.buffer[8] = 'r'
	prg.buffer[9] = 'a'
	prg.buffer[10] = 'n'
	prg.buffer[11] = 's'
	prg.buffer[12] = 'f'
	prg.buffer[13] = 'o'
	prg.buffer[14] = 'r'
	prg.buffer[15] = 'm'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'd'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(binary)

	prg.idFirst = 11
	prg.buffer[11] = 'x'
	prg.buffer[12] = 's'
	prg.buffer[13] = 'c'
	prg.buffer[14] = 'a'
	prg.buffer[15] = 'l'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'd'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(binary)

	prg.idFirst = 11
	prg.buffer[11] = 'y'
	prg.buffer[12] = 's'
	prg.buffer[13] = 'c'
	prg.buffer[14] = 'a'
	prg.buffer[15] = 'l'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'd'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(binary)

	prg.idFirst = 11
	prg.buffer[11] = 'z'
	prg.buffer[12] = 's'
	prg.buffer[13] = 'c'
	prg.buffer[14] = 'a'
	prg.buffer[15] = 'l'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'd'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(binary)

	prg.idFirst = 1
	prg.buffer[1] = 'i'
	prg.buffer[2] = 'n'
	prg.buffer[3] = 't'
	prg.buffer[4] = 'e'
	prg.buffer[5] = 'r'
	prg.buffer[6] = 's'
	prg.buffer[7] = 'e'
	prg.buffer[8] = 'c'
	prg.buffer[9] = 't'
	prg.buffer[10] = 'i'
	prg.buffer[11] = 'o'
	prg.buffer[12] = 'n'
	prg.buffer[13] = 't'
	prg.buffer[14] = 'i'
	prg.buffer[15] = 'm'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 's'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(binary)

	prg.idFirst = 11
	prg.buffer[11] = 'n'
	prg.buffer[12] = 'u'
	prg.buffer[13] = 'm'
	prg.buffer[14] = 'e'
	prg.buffer[15] = 'r'
	prg.buffer[16] = 'i'
	prg.buffer[17] = 'c'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(typeName)

	prg.idFirst = 12
	prg.buffer[12] = 's'
	prg.buffer[13] = 't'
	prg.buffer[14] = 'r'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 'n'
	prg.buffer[17] = 'g'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(typeName)

	prg.idFirst = 11
	prg.buffer[11] = 'b'
	prg.buffer[12] = 'o'
	prg.buffer[13] = 'o'
	prg.buffer[14] = 'l'
	prg.buffer[15] = 'e'
	prg.buffer[16] = 'a'
	prg.buffer[17] = 'n'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(typeName)

	prg.idFirst = 14
	prg.buffer[14] = 'p'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 't'
	prg.buffer[17] = 'h'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(typeName)

	prg.idFirst = 15
	prg.buffer[15] = 'p'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'n'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(typeName)

	prg.idFirst = 11
	prg.buffer[11] = 'p'
	prg.buffer[12] = 'i'
	prg.buffer[13] = 'c'
	prg.buffer[14] = 't'
	prg.buffer[15] = 'u'
	prg.buffer[16] = 'r'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(typeName)

	prg.idFirst = 9
	prg.buffer[9] = 't'
	prg.buffer[10] = 'r'
	prg.buffer[11] = 'a'
	prg.buffer[12] = 'n'
	prg.buffer[13] = 's'
	prg.buffer[14] = 'f'
	prg.buffer[15] = 'o'
	prg.buffer[16] = 'r'
	prg.buffer[17] = 'm'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(typeName)

	prg.idFirst = 14
	prg.buffer[14] = 'p'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 'i'
	prg.buffer[17] = 'r'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(typeName)

	prg.idFirst = 15
	prg.buffer[15] = 'e'
	prg.buffer[16] = 'n'
	prg.buffer[17] = 'd'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(endit)

	prg.idFirst = 14
	prg.buffer[14] = 'd'
	prg.buffer[15] = 'u'
	prg.buffer[16] = 'm'
	prg.buffer[17] = 'p'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(endit)

	prg.idFirst = 9
	prg.buffer[9] = 'b'
	prg.buffer[10] = 'a'
	prg.buffer[11] = 't'
	prg.buffer[12] = 'c'
	prg.buffer[13] = 'h'
	prg.buffer[14] = 'm'
	prg.buffer[15] = 'o'
	prg.buffer[16] = 'd'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(bold)
	prg.idFirst = 7
	prg.buffer[7] = 'n'
	prg.buffer[8] = 'o'
	prg.buffer[9] = 'n'
	prg.buffer[10] = 's'
	prg.buffer[11] = 't'
	prg.buffer[12] = 'o'
	prg.buffer[13] = 'p'
	prg.buffer[14] = 'm'
	prg.buffer[15] = 'o'
	prg.buffer[16] = 'd'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(bold)
	prg.idFirst = 8
	prg.buffer[8] = 's'
	prg.buffer[9] = 'c'
	prg.buffer[10] = 'r'
	prg.buffer[11] = 'o'
	prg.buffer[12] = 'l'
	prg.buffer[13] = 'l'
	prg.buffer[14] = 'm'
	prg.buffer[15] = 'o'
	prg.buffer[16] = 'd'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(bold)
	prg.idFirst = 5
	prg.buffer[5] = 'e'
	prg.buffer[6] = 'r'
	prg.buffer[7] = 'r'
	prg.buffer[8] = 'o'
	prg.buffer[9] = 'r'
	prg.buffer[10] = 's'
	prg.buffer[11] = 't'
	prg.buffer[12] = 'o'
	prg.buffer[13] = 'p'
	prg.buffer[14] = 'm'
	prg.buffer[15] = 'o'
	prg.buffer[16] = 'd'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(bold)
	prg.idFirst = 13
	prg.buffer[13] = 'i'
	prg.buffer[14] = 'n'
	prg.buffer[15] = 'n'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'r'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 13
	prg.buffer[13] = 'o'
	prg.buffer[14] = 'u'
	prg.buffer[15] = 't'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'r'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 9
	prg.buffer[9] = 's'
	prg.buffer[10] = 'h'
	prg.buffer[11] = 'o'
	prg.buffer[12] = 'w'
	prg.buffer[13] = 't'
	prg.buffer[14] = 'o'
	prg.buffer[15] = 'k'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'n'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 9
	prg.buffer[9] = 's'
	prg.buffer[10] = 'h'
	prg.buffer[11] = 'o'
	prg.buffer[12] = 'w'
	prg.buffer[13] = 's'
	prg.buffer[14] = 't'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 't'
	prg.buffer[17] = 's'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(bold)

	prg.idFirst = 14
	prg.buffer[14] = 's'
	prg.buffer[15] = 'h'
	prg.buffer[16] = 'o'
	prg.buffer[17] = 'w'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 6
	prg.buffer[6] = 's'
	prg.buffer[7] = 'h'
	prg.buffer[8] = 'o'
	prg.buffer[9] = 'w'
	prg.buffer[10] = 'v'
	prg.buffer[11] = 'a'
	prg.buffer[12] = 'r'
	prg.buffer[13] = 'i'
	prg.buffer[14] = 'a'
	prg.buffer[15] = 'b'
	prg.buffer[16] = 'l'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 2
	prg.buffer[2] = 's'
	prg.buffer[3] = 'h'
	prg.buffer[4] = 'o'
	prg.buffer[5] = 'w'
	prg.buffer[6] = 'd'
	prg.buffer[7] = 'e'
	prg.buffer[8] = 'p'
	prg.buffer[9] = 'e'
	prg.buffer[10] = 'n'
	prg.buffer[11] = 'd'
	prg.buffer[12] = 'e'
	prg.buffer[13] = 'n'
	prg.buffer[14] = 'c'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 's'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(bold)

	prg.idFirst = 11
	prg.buffer[11] = 'c'
	prg.buffer[12] = 'o'
	prg.buffer[13] = 'n'
	prg.buffer[14] = 't'
	prg.buffer[15] = 'o'
	prg.buffer[16] = 'u'
	prg.buffer[17] = 'r'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 8
	prg.buffer[8] = 'd'
	prg.buffer[9] = 'o'
	prg.buffer[10] = 'u'
	prg.buffer[11] = 'b'
	prg.buffer[12] = 'l'
	prg.buffer[13] = 'e'
	prg.buffer[14] = 'p'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 't'
	prg.buffer[17] = 'h'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 14
	prg.buffer[14] = 'a'
	prg.buffer[15] = 'l'
	prg.buffer[16] = 's'
	prg.buffer[17] = 'o'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 11
	prg.buffer[11] = 'w'
	prg.buffer[12] = 'i'
	prg.buffer[13] = 't'
	prg.buffer[14] = 'h'
	prg.buffer[15] = 'p'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'n'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 8
	prg.buffer[8] = 'w'
	prg.buffer[9] = 'i'
	prg.buffer[10] = 't'
	prg.buffer[11] = 'h'
	prg.buffer[12] = 'w'
	prg.buffer[13] = 'e'
	prg.buffer[14] = 'i'
	prg.buffer[15] = 'g'
	prg.buffer[16] = 'h'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 10
	prg.buffer[10] = 'd'
	prg.buffer[11] = 'r'
	prg.buffer[12] = 'o'
	prg.buffer[13] = 'p'
	prg.buffer[14] = 'p'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 'n'
	prg.buffer[17] = 'g'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 11
	prg.buffer[11] = 'k'
	prg.buffer[12] = 'e'
	prg.buffer[13] = 'e'
	prg.buffer[14] = 'p'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 'n'
	prg.buffer[17] = 'g'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 11
	prg.buffer[11] = 'm'
	prg.buffer[12] = 'e'
	prg.buffer[13] = 's'
	prg.buffer[14] = 's'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 'g'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 8
	prg.buffer[8] = 'e'
	prg.buffer[9] = 'r'
	prg.buffer[10] = 'r'
	prg.buffer[11] = 'm'
	prg.buffer[12] = 'e'
	prg.buffer[13] = 's'
	prg.buffer[14] = 's'
	prg.buffer[15] = 'a'
	prg.buffer[16] = 'g'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 11
	prg.buffer[11] = 'e'
	prg.buffer[12] = 'r'
	prg.buffer[13] = 'r'
	prg.buffer[14] = 'h'
	prg.buffer[15] = 'e'
	prg.buffer[16] = 'l'
	prg.buffer[17] = 'p'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 10
	prg.buffer[10] = 'c'
	prg.buffer[11] = 'h'
	prg.buffer[12] = 'a'
	prg.buffer[13] = 'r'
	prg.buffer[14] = 'l'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 's'
	prg.buffer[17] = 't'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 10
	prg.buffer[10] = 'l'
	prg.buffer[11] = 'i'
	prg.buffer[12] = 'g'
	prg.buffer[13] = 't'
	prg.buffer[14] = 'a'
	prg.buffer[15] = 'b'
	prg.buffer[16] = 'l'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 8
	prg.buffer[8] = 'e'
	prg.buffer[9] = 'x'
	prg.buffer[10] = 't'
	prg.buffer[11] = 'e'
	prg.buffer[12] = 'n'
	prg.buffer[13] = 's'
	prg.buffer[14] = 'i'
	prg.buffer[15] = 'b'
	prg.buffer[16] = 'l'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 8
	prg.buffer[8] = 'h'
	prg.buffer[9] = 'e'
	prg.buffer[10] = 'a'
	prg.buffer[11] = 'd'
	prg.buffer[12] = 'e'
	prg.buffer[13] = 'r'
	prg.buffer[14] = 'b'
	prg.buffer[15] = 'y'
	prg.buffer[16] = 't'
	prg.buffer[17] = 'e'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 9
	prg.buffer[9] = 'f'
	prg.buffer[10] = 'o'
	prg.buffer[11] = 'n'
	prg.buffer[12] = 't'
	prg.buffer[13] = 'd'
	prg.buffer[14] = 'i'
	prg.buffer[15] = 'm'
	prg.buffer[16] = 'e'
	prg.buffer[17] = 'n'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 11
	prg.buffer[11] = 's'
	prg.buffer[12] = 'p'
	prg.buffer[13] = 'e'
	prg.buffer[14] = 'c'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 'a'
	prg.buffer[17] = 'l'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 8
	prg.buffer[8] = 'n'
	prg.buffer[9] = 'u'
	prg.buffer[10] = 'm'
	prg.buffer[11] = 's'
	prg.buffer[12] = 'p'
	prg.buffer[13] = 'e'
	prg.buffer[14] = 'c'
	prg.buffer[15] = 'i'
	prg.buffer[16] = 'a'
	prg.buffer[17] = 'l'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(command)

	prg.idFirst = 17
	prg.buffer[17] = '%'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(comment)

	prg.idFirst = 16
	prg.buffer[16] = '%'
	prg.buffer[17] = '%'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(verbatim)

	prg.idFirst = 15
	prg.buffer[15] = '%'
	prg.buffer[16] = '%'
	prg.buffer[17] = '%'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(setFormat)

	prg.idFirst = 14
	prg.buffer[14] = '%'
	prg.buffer[15] = '%'
	prg.buffer[16] = '%'
	prg.buffer[17] = '%'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(mftComment)

	prg.idFirst = 17
	prg.buffer[17] = '#'
	prg.curTok = int32(prg.lookup())
	prg.ilk[prg.curTok] = uint16(sharp)

	// Store all the translations
	for ii := int32(0); ii <= 255; ii++ {
		prg.i = asciiCode(ii)
		_ = prg.i
		prg.translation[prg.i] = 0
	}
	prg.bytePtr = uint16(int32(prg.bytePtr) + 2)
	prg.byteMem[int32(prg.bytePtr)-2] = '\\'
	prg.byteMem[int32(prg.bytePtr)-1] = '$'
	prg.curTok = int32(prg.namePtr)
	prg.namePtr = uint16(int32(prg.namePtr) + 1)
	prg.byteStart[prg.namePtr] = prg.bytePtr
	prg.translation['$'] = uint16(prg.curTok)

	prg.bytePtr = uint16(int32(prg.bytePtr) + 2)
	prg.byteMem[int32(prg.bytePtr)-2] = '\\'
	prg.byteMem[int32(prg.bytePtr)-1] = '#'
	prg.curTok = int32(prg.namePtr)
	prg.namePtr = uint16(int32(prg.namePtr) + 1)
	prg.byteStart[prg.namePtr] = prg.bytePtr
	prg.translation['#'] = uint16(prg.curTok)

	prg.bytePtr = uint16(int32(prg.bytePtr) + 2)
	prg.byteMem[int32(prg.bytePtr)-2] = '\\'
	prg.byteMem[int32(prg.bytePtr)-1] = '&'
	prg.curTok = int32(prg.namePtr)
	prg.namePtr = uint16(int32(prg.namePtr) + 1)
	prg.byteStart[prg.namePtr] = prg.bytePtr
	prg.translation['&'] = uint16(prg.curTok)

	prg.bytePtr = uint16(int32(prg.bytePtr) + 2)
	prg.byteMem[int32(prg.bytePtr)-2] = '\\'
	prg.byteMem[int32(prg.bytePtr)-1] = '{'
	prg.curTok = int32(prg.namePtr)
	prg.namePtr = uint16(int32(prg.namePtr) + 1)
	prg.byteStart[prg.namePtr] = prg.bytePtr
	prg.translation['{'] = uint16(prg.curTok)

	prg.bytePtr = uint16(int32(prg.bytePtr) + 2)
	prg.byteMem[int32(prg.bytePtr)-2] = '\\'
	prg.byteMem[int32(prg.bytePtr)-1] = '}'
	prg.curTok = int32(prg.namePtr)
	prg.namePtr = uint16(int32(prg.namePtr) + 1)
	prg.byteStart[prg.namePtr] = prg.bytePtr
	prg.translation['}'] = uint16(prg.curTok)

	prg.bytePtr = uint16(int32(prg.bytePtr) + 2)
	prg.byteMem[int32(prg.bytePtr)-2] = '\\'
	prg.byteMem[int32(prg.bytePtr)-1] = '_'
	prg.curTok = int32(prg.namePtr)
	prg.namePtr = uint16(int32(prg.namePtr) + 1)
	prg.byteStart[prg.namePtr] = prg.bytePtr
	prg.translation['_'] = uint16(prg.curTok)

	prg.bytePtr = uint16(int32(prg.bytePtr) + 2)
	prg.byteMem[int32(prg.bytePtr)-2] = '\\'
	prg.byteMem[int32(prg.bytePtr)-1] = '%'
	prg.curTok = int32(prg.namePtr)
	prg.namePtr = uint16(int32(prg.namePtr) + 1)
	prg.byteStart[prg.namePtr] = prg.bytePtr
	prg.translation['%'] = uint16(prg.curTok)

	prg.bytePtr = uint16(int32(prg.bytePtr) + 4)
	prg.byteMem[int32(prg.bytePtr)-4] = '\\'
	prg.byteMem[int32(prg.bytePtr)-3] = 'B'
	prg.byteMem[int32(prg.bytePtr)-2] = 'S'
	prg.byteMem[int32(prg.bytePtr)-1] = ' '
	prg.curTok = int32(prg.namePtr)
	prg.namePtr = uint16(int32(prg.namePtr) + 1)
	prg.byteStart[prg.namePtr] = prg.bytePtr
	prg.translation['\\'] = uint16(prg.curTok)

	prg.bytePtr = uint16(int32(prg.bytePtr) + 4)
	prg.byteMem[int32(prg.bytePtr)-4] = '\\'
	prg.byteMem[int32(prg.bytePtr)-3] = 'H'
	prg.byteMem[int32(prg.bytePtr)-2] = 'A'
	prg.byteMem[int32(prg.bytePtr)-1] = ' '
	prg.curTok = int32(prg.namePtr)
	prg.namePtr = uint16(int32(prg.namePtr) + 1)
	prg.byteStart[prg.namePtr] = prg.bytePtr
	prg.translation['^'] = uint16(prg.curTok)

	prg.bytePtr = uint16(int32(prg.bytePtr) + 4)
	prg.byteMem[int32(prg.bytePtr)-4] = '\\'
	prg.byteMem[int32(prg.bytePtr)-3] = 'T'
	prg.byteMem[int32(prg.bytePtr)-2] = 'I'
	prg.byteMem[int32(prg.bytePtr)-1] = ' '
	prg.curTok = int32(prg.namePtr)
	prg.namePtr = uint16(int32(prg.namePtr) + 1)
	prg.byteStart[prg.namePtr] = prg.bytePtr
	prg.translation['~'] = uint16(prg.curTok)

	prg.bytePtr = uint16(int32(prg.bytePtr) + 5)
	prg.byteMem[int32(prg.bytePtr)-5] = '\\'
	prg.byteMem[int32(prg.bytePtr)-4] = 'a'
	prg.byteMem[int32(prg.bytePtr)-3] = 's'
	prg.byteMem[int32(prg.bytePtr)-2] = 't'
	prg.byteMem[int32(prg.bytePtr)-1] = ' '
	prg.curTok = int32(prg.namePtr)
	prg.namePtr = uint16(int32(prg.namePtr) + 1)
	prg.byteStart[prg.namePtr] = prg.bytePtr
	prg.translation['*'] = uint16(prg.curTok)

	prg.bytePtr = uint16(int32(prg.bytePtr) + 4)
	prg.byteMem[int32(prg.bytePtr)-4] = '\\'
	prg.byteMem[int32(prg.bytePtr)-3] = 'A'
	prg.byteMem[int32(prg.bytePtr)-2] = 'M'
	prg.byteMem[int32(prg.bytePtr)-1] = ' '
	prg.curTok = int32(prg.namePtr)
	prg.namePtr = uint16(int32(prg.namePtr) + 1)
	prg.byteStart[prg.namePtr] = prg.bytePtr
	prg.trAmp = uint16(prg.curTok)

	// \xref[\\AM, etc]
	prg.bytePtr = uint16(int32(prg.bytePtr) + 4)
	prg.byteMem[int32(prg.bytePtr)-4] = '\\'
	prg.byteMem[int32(prg.bytePtr)-3] = 'B'
	prg.byteMem[int32(prg.bytePtr)-2] = 'L'
	prg.byteMem[int32(prg.bytePtr)-1] = ' '
	prg.curTok = int32(prg.namePtr)
	prg.namePtr = uint16(int32(prg.namePtr) + 1)
	prg.byteStart[prg.namePtr] = prg.bytePtr
	prg.trSkip = uint16(prg.curTok)

	prg.bytePtr = uint16(int32(prg.bytePtr) + 4)
	prg.byteMem[int32(prg.bytePtr)-4] = '\\'
	prg.byteMem[int32(prg.bytePtr)-3] = 'S'
	prg.byteMem[int32(prg.bytePtr)-2] = 'H'
	prg.byteMem[int32(prg.bytePtr)-1] = ' '
	prg.curTok = int32(prg.namePtr)
	prg.namePtr = uint16(int32(prg.namePtr) + 1)
	prg.byteStart[prg.namePtr] = prg.bytePtr
	prg.trSharp = uint16(prg.curTok)

	prg.bytePtr = uint16(int32(prg.bytePtr) + 4)
	prg.byteMem[int32(prg.bytePtr)-4] = '\\'
	prg.byteMem[int32(prg.bytePtr)-3] = 'P'
	prg.byteMem[int32(prg.bytePtr)-2] = 'S'
	prg.byteMem[int32(prg.bytePtr)-1] = ' '
	prg.curTok = int32(prg.namePtr)
	prg.namePtr = uint16(int32(prg.namePtr) + 1)
	prg.byteStart[prg.namePtr] = prg.bytePtr
	prg.trPs = uint16(prg.curTok)

	prg.bytePtr = uint16(int32(prg.bytePtr) + 4)
	prg.byteMem[int32(prg.bytePtr)-4] = '\\'
	prg.byteMem[int32(prg.bytePtr)-3] = 'l'
	prg.byteMem[int32(prg.bytePtr)-2] = 'e'
	prg.byteMem[int32(prg.bytePtr)-1] = ' '
	prg.curTok = int32(prg.namePtr)
	prg.namePtr = uint16(int32(prg.namePtr) + 1)
	prg.byteStart[prg.namePtr] = prg.bytePtr
	prg.trLe = uint16(prg.curTok)

	prg.bytePtr = uint16(int32(prg.bytePtr) + 4)
	prg.byteMem[int32(prg.bytePtr)-4] = '\\'
	prg.byteMem[int32(prg.bytePtr)-3] = 'g'
	prg.byteMem[int32(prg.bytePtr)-2] = 'e'
	prg.byteMem[int32(prg.bytePtr)-1] = ' '
	prg.curTok = int32(prg.namePtr)
	prg.namePtr = uint16(int32(prg.namePtr) + 1)
	prg.byteStart[prg.namePtr] = prg.bytePtr
	prg.trGe = uint16(prg.curTok)

	prg.bytePtr = uint16(int32(prg.bytePtr) + 4)
	prg.byteMem[int32(prg.bytePtr)-4] = '\\'
	prg.byteMem[int32(prg.bytePtr)-3] = 'n'
	prg.byteMem[int32(prg.bytePtr)-2] = 'e'
	prg.byteMem[int32(prg.bytePtr)-1] = ' '
	prg.curTok = int32(prg.namePtr)
	prg.namePtr = uint16(int32(prg.namePtr) + 1)
	prg.byteStart[prg.namePtr] = prg.bytePtr
	prg.trNe = uint16(prg.curTok)

	prg.bytePtr = uint16(int32(prg.bytePtr) + 5)
	prg.byteMem[int32(prg.bytePtr)-5] = '\\'
	prg.byteMem[int32(prg.bytePtr)-4] = 'q'
	prg.byteMem[int32(prg.bytePtr)-3] = 'u'
	prg.byteMem[int32(prg.bytePtr)-2] = 'a'
	prg.byteMem[int32(prg.bytePtr)-1] = 'd'
	prg.curTok = int32(prg.namePtr)
	prg.namePtr = uint16(int32(prg.namePtr) + 1)
	prg.byteStart[prg.namePtr] = prg.bytePtr
	prg.trQuad = uint16(prg.curTok)

	// Initialize the input...
	{
		prg.openInput()
		prg.line = 0
		prg.otherLine = 0

		prg.changing = true
		prg.primeTheChangeBuffer()
		prg.changing = !prg.changing
		prg.tempLine = prg.otherLine
		prg.otherLine = prg.line
		prg.line = prg.tempLine

		prg.styling = true
		prg.limit = 0
		prg.loc = 1
		prg.buffer[0] = ' '
		prg.inputHasEnded = false
	}
	prg.doTheTranslation()

	// Check that all changes have been read
	if int32(prg.changeLimit) != 0 {
		for ii := int32(0); ii <= int32(prg.changeLimit)-1; ii++ {
			prg.loc = byte(ii)
			_ = prg.loc
			prg.buffer[prg.loc] = prg.changeBuffer[prg.loc]
		}
		prg.limit = prg.changeLimit
		prg.changing = true
		prg.line = prg.otherLine
		prg.loc = prg.changeLimit
		{
			prg.stdout.Writeln()
			prg.stdout.Write("! Change file entry did not match")
			prg.error1()
		}
		// \xref[Change file entry did not match]
	}

	// Print the job |history|
	switch prg.history {
	case spotless:
		prg.stdout.Writeln()
		prg.stdout.Write("(No errors were found.)")
	case harmlessMessage:
		prg.stdout.Writeln()
		prg.stdout.Write("(Did you see the warning message above?)")
	case errorMessage:
		prg.stdout.Writeln()
		prg.stdout.Write("(Pardon me, but I think I spotted something wrong.)")
	case fatalMessage:
		prg.stdout.Writeln()
		prg.stdout.Write("(That was a fatal error, my friend.)")
	} // there are no other cases
	prg.stdout.Writeln("")

}

// 114. System-dependent changes

// tangle:pos mft.web:1970:29:

// This module should be replaced, if necessary, by changes to the program
// that are necessary to make \.[MFT] work at a particular installation.
// It is usually best to design your change file so that all changes to
// previous modules preserve the module numbering; then everybody's version
// will be consistent with the printed program. More extensive changes,
// which introduce new modules, can be inserted here; then only the index
// itself will get a new module number.
// \xref[system dependencies]

// 115. Index
