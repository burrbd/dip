version: 2
jobs:
  test:
    working_directory: ~/project
    docker:
    - image: cimg/go:1.24
    steps:
    - checkout
    - run: go test -v -cover -race ./...
workflows:
  version: 2
  build-workflow:
    jobs:
      - test

